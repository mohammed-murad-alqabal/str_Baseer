# Requirements Document - نظام الترجمة والتدويل

**المشروع:** بصير MVP  
**التاريخ:** 8 ديسمبر 2025  
**المؤلف:** فريق وكلاء تطوير مشروع بصير  
**الحالة:** 🔄 قيد المراجعة

---

## Introduction

بناءً على نتائج اختبار I18n Testing Script، تم اكتشاف عدة مشاكل في دعم التدويل والترجمة في تطبيق بصير MVP. هذا المستند يحدد المتطلبات لبناء نظام شامل للترجمة والتدويل يدعم اللغتين العربية والإنجليزية مع إمكانية التوسع لدعم لغات إضافية في المستقبل.

### المشاكل المكتشفة

1. ❌ لا توجد ملفات ترجمة (ARB files)
2. ⚠️ 191 نص مشفر (hardcoded strings)
3. ⚠️ 7 alignments مشفرة (left/right بدلاً من start/end)
4. ⚠️ 191 widget بدون overflow handling
5. ⚠️ لا يوجد استخدام لـ DateFormat/NumberFormat

---

## Glossary

- **I18n System**: نظام الترجمة والتدويل في تطبيق بصير
- **I18n (Internationalization)**: عملية تصميم التطبيق ليدعم لغات متعددة
- **L10n (Localization)**: عملية ترجمة التطبيق للغة محددة
- **ARB (Application Resource Bundle)**: صيغة ملفات الترجمة في Flutter
- **RTL (Right-to-Left)**: اتجاه الكتابة من اليمين لليسار (العربية)
- **LTR (Left-to-Right)**: اتجاه الكتابة من اليسار لليمين (الإنجليزية)
- **Locale**: معرف اللغة والمنطقة (مثل: ar_SA, en_US)
- **Translation Key**: مفتاح فريد لكل نص قابل للترجمة
- **Fallback Locale**: اللغة الافتراضية عند عدم توفر الترجمة
- **Localization Service**: خدمة إدارة اللغة والترجمة في التطبيق

---

## Requirements

### Requirement 1: إعداد بنية الترجمة الأساسية

**User Story:** كمطور، أريد بنية أساسية للترجمة، حتى أتمكن من إدارة النصوص بلغات متعددة بشكل منظم.

#### Acceptance Criteria

1. WHEN المطور يُعد المشروع THEN THE I18n System SHALL ينشئ مجلد `lib/l10n` لملفات الترجمة
2. WHEN ملفات ARB تُنشأ THEN THE I18n System SHALL يدعم اللغتين العربية (ar) والإنجليزية (en) كحد أدنى
3. WHEN ملف `l10n.yaml` يُنشأ THEN THE I18n System SHALL يحدد إعدادات التوليد التلقائي للترجمات
4. WHEN التطبيق يُبنى THEN THE I18n System SHALL يولد ملف `app_localizations.dart` تلقائياً
5. WHEN المطور يضيف مفتاح ترجمة جديد THEN THE I18n System SHALL يتحقق من وجوده في جميع ملفات اللغات

---

### Requirement 2: استبدال النصوص المشفرة

**User Story:** كمطور، أريد استبدال جميع النصوص المشفرة بمفاتيح ترجمة، حتى يكون التطبيق قابلاً للترجمة بالكامل.

#### Acceptance Criteria

1. WHEN المطور يفحص الكود THEN THE I18n System SHALL يحدد جميع النصوص المشفرة (191 نص)
2. WHEN نص مشفر يُستبدل THEN THE I18n System SHALL يستخدم `AppLocalizations.of(context)` للوصول للترجمة
3. WHEN مفتاح ترجمة يُضاف THEN THE I18n System SHALL يضيفه في جميع ملفات ARB المدعومة
4. WHEN ترجمة مفقودة THEN THE I18n System SHALL يستخدم النص العربي كـ fallback
5. WHEN المطور يبحث عن نص THEN THE I18n System SHALL يوفر أداة للبحث في ملفات الترجمة

---

### Requirement 3: دعم RTL/LTR الكامل

**User Story:** كمستخدم، أريد واجهة تتكيف مع اتجاه اللغة، حتى تكون التجربة طبيعية في كلا الاتجاهين.

#### Acceptance Criteria

1. WHEN اللغة العربية مفعلة THEN THE I18n System SHALL يعرض الواجهة بـ RTL
2. WHEN اللغة الإنجليزية مفعلة THEN THE I18n System SHALL يعرض الواجهة بـ LTR
3. WHEN alignment يُستخدم في widget THEN THE I18n System SHALL يستخدم `start/end` بدلاً من `left/right`
4. WHEN padding يُستخدم في widget THEN THE I18n System SHALL يستخدم `EdgeInsetsDirectional` بدلاً من `EdgeInsets`
5. WHEN icon يُعرض في RTL mode THEN THE I18n System SHALL ينعكس تلقائياً إذا لزم الأمر

---

### Requirement 4: معالجة طول النصوص

**User Story:** كمستخدم، أريد أن تُعرض النصوص الطويلة بشكل صحيح، حتى لا تتجاوز حدود الشاشة.

#### Acceptance Criteria

1. WHEN نص طويل يُعرض في Text widget THEN THE I18n System SHALL يستخدم `overflow: TextOverflow.ellipsis`
2. WHEN نص متعدد الأسطر يُعرض THEN THE I18n System SHALL يحدد `maxLines` مناسب
3. WHEN نص يتجاوز الحد المتاح THEN THE I18n System SHALL يعرض "..." في النهاية
4. WHEN مساحة محدودة متاحة للنص THEN THE I18n System SHALL يستخدم `Flexible` أو `Expanded` حسب الحاجة
5. WHEN نص يُعرض في زر THEN THE I18n System SHALL يضمن عدم تجاوز حدود الزر

---

### Requirement 5: تنسيق التواريخ والأرقام

**User Story:** كمستخدم، أريد رؤية التواريخ والأرقام بتنسيق مناسب للغتي، حتى تكون المعلومات واضحة ومألوفة.

#### Acceptance Criteria

1. WHEN تاريخ يُعرض للمستخدم THEN THE I18n System SHALL يستخدم `DateFormat` مع locale المناسب
2. WHEN رقم يُعرض للمستخدم THEN THE I18n System SHALL يستخدم `NumberFormat` مع locale المناسب
3. WHEN مبلغ مالي يُعرض THEN THE I18n System SHALL يستخدم `NumberFormat.currency` مع رمز العملة المناسب
4. WHEN تاريخ نسبي يُعرض بالعربية THEN THE I18n System SHALL يعرض "منذ ساعة"، "قبل يومين" بالصيغة الصحيحة
5. WHEN أرقام عربية مطلوبة THEN THE I18n System SHALL يوفر خيار لتحويل الأرقام الإنجليزية للعربية

---

### Requirement 6: تبديل اللغة

**User Story:** كمستخدم، أريد تبديل لغة التطبيق بسهولة، حتى أستخدم اللغة التي أفضلها.

#### Acceptance Criteria

1. WHEN المستخدم يفتح شاشة الإعدادات THEN THE Localization Service SHALL يعرض خيار تغيير اللغة
2. WHEN المستخدم يختار لغة جديدة THEN THE Localization Service SHALL يحفظ الاختيار في التخزين المحلي
3. WHEN اللغة تتغير THEN THE Localization Service SHALL يعيد بناء الواجهة بالكامل
4. WHEN التطبيق يُفتح THEN THE Localization Service SHALL يستخدم آخر لغة محفوظة
5. WHEN لغة النظام تتغير AND المستخدم لم يختر لغة محددة THEN THE Localization Service SHALL يتبع لغة النظام

---

### Requirement 7: التحقق من اكتمال الترجمات

**User Story:** كمطور، أريد أداة للتحقق من اكتمال الترجمات، حتى أضمن عدم وجود نصوص مفقودة.

#### Acceptance Criteria

1. WHEN سكريبت التحقق يُشغل THEN THE I18n System SHALL يقارن جميع ملفات ARB
2. WHEN مفتاح ترجمة مفقود في لغة THEN THE I18n System SHALL يعرض تحذير واضح
3. WHEN ترجمة فارغة موجودة THEN THE I18n System SHALL يعتبرها مفقودة
4. WHEN جميع الترجمات مكتملة THEN THE I18n System SHALL يعرض رسالة نجاح
5. WHEN تقرير التحقق يُنشأ THEN THE I18n System SHALL يحفظه في `test_results/i18n/`

---

### Requirement 8: اختبارات الترجمة

**User Story:** كمطور، أريد اختبارات تلقائية للترجمة، حتى أكتشف المشاكل مبكراً.

#### Acceptance Criteria

1. WHEN اختبار الترجمة يُشغل THEN THE I18n System SHALL يتحقق من وجود جميع مفاتيح الترجمة
2. WHEN widget يُختبر THEN THE I18n System SHALL يختبره بكلا اللغتين (العربية والإنجليزية)
3. WHEN RTL support يُختبر THEN THE I18n System SHALL يتحقق من صحة الاتجاه
4. WHEN text overflow يُختبر THEN THE I18n System SHALL يتحقق من معالجة النصوص الطويلة
5. WHEN تنسيق التواريخ والأرقام يُختبر THEN THE I18n System SHALL يتحقق من DateFormat و NumberFormat

---

### Requirement 9: توثيق نظام الترجمة

**User Story:** كمطور جديد، أريد توثيق واضح لنظام الترجمة، حتى أفهم كيفية إضافة ترجمات جديدة.

#### Acceptance Criteria

1. WHEN مطور يبحث عن التوثيق THEN THE I18n System SHALL يوفر دليل شامل في `.kiro/docs/`
2. WHEN مطور يضيف نص جديد THEN THE I18n System SHALL يوفر توثيق يشرح الخطوات بالتفصيل
3. WHEN مطور يضيف لغة جديدة THEN THE I18n System SHALL يوفر توثيق يشرح العملية الكاملة
4. WHEN مشكلة تحدث THEN THE I18n System SHALL يوفر توثيق يحتوي على قسم troubleshooting
5. WHEN مطور يحتاج أمثلة THEN THE I18n System SHALL يوفر توثيق يحتوي على أمثلة عملية لكل حالة

---

### Requirement 10: أدوات المطور

**User Story:** كمطور، أريد أدوات تساعدني في إدارة الترجمات، حتى أعمل بكفاءة أعلى.

#### Acceptance Criteria

1. WHEN مطور يحتاج استخراج نصوص مشفرة THEN THE I18n System SHALL يوفر سكريبت لاستخراج النصوص المشفرة
2. WHEN مطور يحتاج مزامنة مفاتيح THEN THE I18n System SHALL يوفر سكريبت لمزامنة مفاتيح الترجمة
3. WHEN مطور يحتاج إحصائيات THEN THE I18n System SHALL يوفر تقرير بنسبة اكتمال كل لغة
4. WHEN مطور يحتاج بحث عن ترجمة THEN THE I18n System SHALL يوفر أداة للبحث في جميع الترجمات
5. WHEN مطور يحتاج تصدير ترجمات THEN THE I18n System SHALL يوفر خيار لتصدير الترجمات بصيغ مختلفة

---

## Technical Constraints

### Flutter & Dart

- Flutter 3.24.0+
- Dart 3.5.0+
- Package `flutter_localizations`
- Package `intl` ^0.18.0

### ملفات الترجمة

- صيغة ARB (Application Resource Bundle)
- UTF-8 encoding
- JSON structure

### الأداء

- تحميل الترجمات يجب أن يكون < 100ms
- تبديل اللغة يجب أن يكون < 500ms
- حجم ملفات ARB يجب أن يكون < 100KB لكل لغة

### التوافق

- دعم Android 5.0+
- دعم iOS 12.0+
- دعم Web (Chrome, Safari, Firefox)

---

## Non-Functional Requirements

### الأداء

- تحميل الترجمات يجب أن يكون سريع (< 100ms)
- تبديل اللغة يجب أن يكون سلس (< 500ms)
- لا تأثير ملحوظ على أداء التطبيق

### قابلية الصيانة

- بنية واضحة ومنظمة
- تسمية موحدة للمفاتيح
- تجميع منطقي للترجمات

### قابلية التوسع

- سهولة إضافة لغات جديدة
- دعم pluralization
- دعم gender-specific translations

### الأمان

- لا بيانات حساسة في ملفات الترجمة
- التحقق من صحة المدخلات
- حماية من injection attacks

---

## Success Metrics

### التغطية

- 100% من النصوص المرئية مترجمة
- 0 نصوص مشفرة في الكود
- 100% من الشاشات تدعم RTL/LTR

### الجودة

- 0 مفاتيح ترجمة مفقودة
- 0 استخدام لـ left/right alignment
- 100% من Text widgets لديها overflow handling

### الاختبارات

- 100% من اختبارات الترجمة تنجح
- تغطية اختبارية > 80% لكود الترجمة
- 0 regression bugs متعلقة بالترجمة

---

## Out of Scope

### المرحلة الأولى (MVP)

- ❌ دعم أكثر من لغتين (عربي/إنجليزي)
- ❌ ترجمة آلية (Machine Translation)
- ❌ ترجمة المحتوى الديناميكي من المستخدمين
- ❌ دعم لهجات مختلفة (مصري، خليجي، إلخ)
- ❌ تحويل تلقائي للأرقام العربية/الهندية

### المستقبل

- ⏳ دعم لغات إضافية (فرنسي، ألماني، إلخ)
- ⏳ نظام إدارة ترجمات عبر الإنترنت
- ⏳ تكامل مع خدمات الترجمة الاحترافية
- ⏳ A/B testing للترجمات
- ⏳ تحليلات استخدام اللغات

---

**تم إعداده بواسطة:** فريق وكلاء تطوير مشروع بصير  
**التاريخ:** 8 ديسمبر 2025  
**الإصدار:** 1.0  
**الحالة:** 🔄 قيد المراجعة
