# 🔄 تدفقات المستخدم - مشروع "بصير" (User Flows)

## معلومات الوثيقة
```yaml
document_type: "User Flows Documentation"
version: "1.0"
created_date: "2025-10-11"
last_updated: "2025-10-11"
status: "Active"
classification: "Internal"
owner: "فريق تطوير مشروع بصير"
```

## 🎯 نظرة عامة (Overview)

تحدد هذه الوثيقة جميع تدفقات المستخدم الأساسية في منصة "بصير"، مع التركيز على تجربة المستخدم العربي وسهولة الاستخدام.

## 👤 التدفقات الأساسية للمستخدم (Core User Flows)

### UF-001: تسجيل مستخدم جديد (New User Registration)

```mermaid
flowchart TD
    A[زيارة الموقع] --> B{مستخدم جديد؟}
    B -->|نعم| C[النقر على 'إنشاء حساب']
    B -->|لا| D[تسجيل الدخول]
    
    C --> E[إدخال البيانات الأساسية]
    E --> F[اختيار نوع الحساب]
    F --> G{نوع الحساب}
    
    G -->|فردي| H[بيانات شخصية]
    G -->|تجاري| I[بيانات الشركة]
    
    H --> J[التحقق من الهاتف]
    I --> J
    
    J --> K[إعداد كلمة المرور]
    K --> L[قبول الشروط والأحكام]
    L --> M[إرسال رمز التحقق]
    M --> N[إدخال رمز التحقق]
    N --> O{الرمز صحيح؟}
    
    O -->|نعم| P[إنشاء الحساب بنجاح]
    O -->|لا| Q[رسالة خطأ]
    Q --> N
    
    P --> R[الانتقال لجولة تعريفية]
    R --> S[الانتقال للوحة التحكم]
```

**تفاصيل التدفق:**
```yaml
flow_details:
  estimated_time: "3-5 minutes"
  steps_count: 8
  drop_off_points:
    - "إدخال بيانات الشركة (15% drop-off)"
    - "التحقق من الهاتف (10% drop-off)"
  
  success_criteria:
    - "معدل إكمال التسجيل > 80%"
    - "وقت التسجيل < 5 دقائق"
    - "معدل أخطاء < 5%"
  
  error_handling:
    - "رسائل خطأ واضحة بالعربية"
    - "إمكانية تصحيح البيانات"
    - "دعم فوري عند الحاجة"
```

### UF-002: إنشاء فاتورة جديدة (Create New Invoice)

```mermaid
flowchart TD
    A[الدخول للوحة التحكم] --> B[النقر على 'فاتورة جديدة']
    B --> C[اختيار نوع الفاتورة]
    C --> D{نوع الفاتورة}
    
    D -->|فاتورة مبيعات| E[نموذج فاتورة المبيعات]
    D -->|فاتورة مردودات| F[نموذج فاتورة المردودات]
    D -->|إشعار دائن| G[نموذج إشعار دائن]
    
    E --> H[إدخال بيانات العميل]
    F --> H
    G --> H
    
    H --> I{عميل موجود؟}
    I -->|نعم| J[اختيار من القائمة]
    I -->|لا| K[إضافة عميل جديد]
    
    J --> L[إضافة المنتجات/الخدمات]
    K --> L
    
    L --> M[حساب الضرائب تلقائياً]
    M --> N[مراجعة الفاتورة]
    N --> O{البيانات صحيحة؟}
    
    O -->|لا| P[تعديل البيانات]
    P --> N
    
    O -->|نعم| Q[حفظ الفاتورة]
    Q --> R[إرسال إلى ZATCA]
    R --> S{استجابة ZATCA}
    
    S -->|مقبولة| T[الفاتورة معتمدة]
    S -->|مرفوضة| U[رسالة خطأ]
    
    T --> V[إرسال للعميل]
    U --> W[تصحيح الأخطاء]
    W --> R
    
    V --> X[تحديث السجلات]
    X --> Y[العودة للوحة التحكم]
```

**تفاصيل التدفق:**
```yaml
flow_details:
  estimated_time: "2-3 minutes"
  steps_count: 12
  automation_level: "70%"
  
  success_criteria:
    - "إنشاء فاتورة في < 2 دقيقة"
    - "معدل نجاح ZATCA > 99%"
    - "معدل أخطاء < 1%"
  
  smart_features:
    - "اقتراح العملاء تلقائياً"
    - "حساب الضرائب تلقائياً"
    - "حفظ المسودات تلقائياً"
    - "التحقق من البيانات قبل الإرسال"
```

### UF-003: إدارة العملاء (Customer Management)

```mermaid
flowchart TD
    A[الدخول لقسم العملاء] --> B{الإجراء المطلوب}
    
    B -->|إضافة عميل جديد| C[نموذج عميل جديد]
    B -->|البحث عن عميل| D[شريط البحث]
    B -->|عرض قائمة العملاء| E[قائمة العملاء]
    
    C --> F[إدخال البيانات الأساسية]
    F --> G{نوع العميل}
    
    G -->|فرد| H[بيانات شخصية]
    G -->|شركة| I[بيانات تجارية]
    
    H --> J[حفظ العميل]
    I --> J
    
    D --> K[إدخال معايير البحث]
    K --> L[عرض النتائج]
    L --> M[اختيار العميل]
    
    E --> N[عرض قائمة مرتبة]
    N --> O[اختيار عميل]
    
    M --> P[صفحة تفاصيل العميل]
    O --> P
    J --> P
    
    P --> Q{الإجراء المطلوب}
    Q -->|تعديل البيانات| R[تحديث المعلومات]
    Q -->|عرض الفواتير| S[تاريخ المعاملات]
    Q -->|إنشاء فاتورة| T[فاتورة جديدة للعميل]
    
    R --> U[حفظ التغييرات]
    S --> V[عرض التقارير]
    T --> W[نموذج الفاتورة]
```

### UF-004: عرض التقارير والتحليلات (Reports & Analytics)

```mermaid
flowchart TD
    A[الدخول لقسم التقارير] --> B[اختيار نوع التقرير]
    B --> C{نوع التقرير}
    
    C -->|تقرير المبيعات| D[معايير تقرير المبيعات]
    C -->|تقرير العملاء| E[معايير تقرير العملاء]
    C -->|تقرير ضريبي| F[معايير التقرير الضريبي]
    C -->|تقرير مخصص| G[بناء تقرير مخصص]
    
    D --> H[اختيار الفترة الزمنية]
    E --> H
    F --> H
    G --> I[اختيار الحقول والمعايير]
    
    H --> J[تطبيق المرشحات]
    I --> J
    
    J --> K[إنتاج التقرير]
    K --> L[عرض النتائج]
    L --> M{راضٍ عن النتائج؟}
    
    M -->|لا| N[تعديل المعايير]
    N --> J
    
    M -->|نعم| O[خيارات التصدير]
    O --> P{تصدير التقرير؟}
    
    P -->|نعم| Q[اختيار صيغة التصدير]
    P -->|لا| R[حفظ التقرير]
    
    Q --> S{صيغة التصدير}
    S -->|PDF| T[تصدير PDF]
    S -->|Excel| U[تصدير Excel]
    S -->|CSV| V[تصدير CSV]
    
    T --> W[تحميل الملف]
    U --> W
    V --> W
    R --> X[العودة للتقارير]
```

## 📱 تدفقات التطبيق المحمول (Mobile App Flows)

### UF-005: مسح الإيصالات (Receipt Scanning)

```mermaid
flowchart TD
    A[فتح التطبيق] --> B[النقر على 'مسح إيصال']
    B --> C{إذن الكاميرا}
    
    C -->|مرفوض| D[طلب الإذن]
    C -->|مقبول| E[فتح الكاميرا]
    
    D --> F{منح الإذن؟}
    F -->|لا| G[رسالة خطأ]
    F -->|نعم| E
    
    E --> H[توجيه الكاميرا للإيصال]
    H --> I[التقاط الصورة]
    I --> J[معالجة الصورة]
    J --> K[استخلاص البيانات بـ OCR]
    
    K --> L{نجح الاستخلاص؟}
    L -->|لا| M[إعادة التقاط]
    L -->|نعم| N[عرض البيانات المستخلصة]
    
    M --> H
    
    N --> O[تأكيد/تعديل البيانات]
    O --> P[تصنيف المصروف]
    P --> Q[حفظ المصروف]
    Q --> R[إضافة للسجلات]
    R --> S[العودة للوحة التحكم]
```

### UF-006: الإشعارات والتنبيهات (Notifications)

```mermaid
flowchart TD
    A[استلام إشعار] --> B{نوع الإشعار}
    
    B -->|فاتورة جديدة| C[عرض تفاصيل الفاتورة]
    B -->|دفعة مستحقة| D[عرض المبلغ المستحق]
    B -->|تحديث ZATCA| E[عرض التحديث]
    B -->|تنبيه أمني| F[عرض التنبيه الأمني]
    
    C --> G[خيارات الإجراء]
    D --> H[خيارات الدفع]
    E --> I[تطبيق التحديث]
    F --> J[إجراءات الأمان]
    
    G --> K{الإجراء المختار}
    K -->|عرض| L[فتح الفاتورة]
    K -->|تجاهل| M[إغلاق الإشعار]
    K -->|تأجيل| N[تذكير لاحقاً]
    
    H --> O{طريقة الدفع}
    O -->|دفع فوري| P[معالجة الدفع]
    O -->|تذكير لاحقاً| Q[جدولة التذكير]
    
    I --> R[إعادة تشغيل التطبيق]
    J --> S[تطبيق إجراءات الأمان]
```

## 🔐 تدفقات الأمان (Security Flows)

### UF-007: تسجيل الدخول الآمن (Secure Login)

```mermaid
flowchart TD
    A[فتح التطبيق] --> B{مسجل دخول؟}
    
    B -->|نعم| C[التحقق من الجلسة]
    B -->|لا| D[شاشة تسجيل الدخول]
    
    C --> E{الجلسة صالحة؟}
    E -->|نعم| F[الانتقال للوحة التحكم]
    E -->|لا| D
    
    D --> G[إدخال البيانات]
    G --> H[التحقق من البيانات]
    H --> I{البيانات صحيحة؟}
    
    I -->|لا| J[رسالة خطأ]
    J --> G
    
    I -->|نعم| K[إرسال رمز التحقق]
    K --> L[إدخال رمز التحقق]
    L --> M{الرمز صحيح؟}
    
    M -->|لا| N[رسالة خطأ]
    N --> L
    
    M -->|نعم| O{المصادقة البيومترية متاحة؟}
    O -->|نعم| P[تفعيل المصادقة البيومترية]
    O -->|لا| Q[إنشاء الجلسة]
    
    P --> Q
    Q --> F
```

### UF-008: إعادة تعيين كلمة المرور (Password Reset)

```mermaid
flowchart TD
    A[النقر على 'نسيت كلمة المرور'] --> B[إدخال البريد الإلكتروني]
    B --> C[التحقق من وجود الحساب]
    C --> D{الحساب موجود؟}
    
    D -->|لا| E[رسالة 'البريد غير مسجل']
    D -->|نعم| F[إرسال رابط إعادة التعيين]
    
    E --> B
    F --> G[رسالة تأكيد الإرسال]
    G --> H[فتح البريد الإلكتروني]
    H --> I[النقر على الرابط]
    I --> J[التحقق من صحة الرابط]
    
    J --> K{الرابط صالح؟}
    K -->|لا| L[رسالة 'الرابط منتهي الصلاحية']
    K -->|نعم| M[صفحة كلمة مرور جديدة]
    
    L --> A
    M --> N[إدخال كلمة المرور الجديدة]
    N --> O[تأكيد كلمة المرور]
    O --> P[التحقق من قوة كلمة المرور]
    
    P --> Q{كلمة المرور قوية؟}
    Q -->|لا| R[رسالة تحسين كلمة المرور]
    Q -->|نعم| S[حفظ كلمة المرور الجديدة]
    
    R --> N
    S --> T[رسالة نجاح التغيير]
    T --> U[تسجيل الدخول بكلمة المرور الجديدة]
```

## 📊 تدفقات التحليلات (Analytics Flows)

### UF-009: عرض لوحة التحكم (Dashboard View)

```mermaid
flowchart TD
    A[تسجيل الدخول] --> B[تحميل بيانات لوحة التحكم]
    B --> C[عرض المؤشرات الرئيسية]
    C --> D[عرض الرسوم البيانية]
    D --> E[عرض الإشعارات الحديثة]
    
    E --> F{تفاعل المستخدم}
    F -->|النقر على مؤشر| G[عرض تفاصيل المؤشر]
    F -->|النقر على رسم بياني| H[عرض الرسم بالتفصيل]
    F -->|النقر على إشعار| I[فتح الإشعار]
    F -->|تحديث البيانات| J[إعادة تحميل البيانات]
    
    G --> K[عرض البيانات التفصيلية]
    H --> L[عرض الرسم التفاعلي]
    I --> M[معالجة الإشعار]
    J --> B
    
    K --> N[خيارات إضافية]
    L --> O[أدوات التحليل]
    M --> P[تحديث حالة الإشعار]
    
    N --> Q{الإجراء المطلوب}
    Q -->|تصدير البيانات| R[تصدير التقرير]
    Q -->|مشاركة| S[مشاركة المؤشر]
    Q -->|حفظ| T[حفظ في المفضلة]
```

## 🔄 تدفقات الأخطاء والاستثناءات (Error Handling Flows)

### UF-010: معالجة أخطاء الشبكة (Network Error Handling)

```mermaid
flowchart TD
    A[محاولة إجراء عملية] --> B[فحص الاتصال]
    B --> C{الاتصال متاح؟}
    
    C -->|نعم| D[تنفيذ العملية]
    C -->|لا| E[عرض رسالة عدم الاتصال]
    
    D --> F{نجحت العملية؟}
    F -->|نعم| G[عرض رسالة النجاح]
    F -->|لا| H[تحليل نوع الخطأ]
    
    E --> I[خيارات المستخدم]
    I --> J{الإجراء المختار}
    J -->|إعادة المحاولة| A
    J -->|العمل أوفلاين| K[تفعيل الوضع الأوفلاين]
    J -->|إلغاء| L[العودة للشاشة السابقة]
    
    H --> M{نوع الخطأ}
    M -->|خطأ خادم| N[رسالة خطأ خادم]
    M -->|خطأ مصادقة| O[إعادة تسجيل الدخول]
    M -->|خطأ بيانات| P[رسالة تصحيح البيانات]
    
    N --> Q[خيار إعادة المحاولة]
    O --> R[تسجيل دخول جديد]
    P --> S[تعديل البيانات]
    
    K --> T[حفظ البيانات محلياً]
    T --> U[مزامنة عند عودة الاتصال]
```

## 📋 معايير تقييم التدفقات (Flow Evaluation Criteria)

### معايير الأداء
```yaml
performance_criteria:
  loading_time: "< 3 seconds per screen"
  transition_time: "< 300ms between screens"
  error_recovery: "< 5 seconds to recover from errors"
  offline_capability: "Core functions work offline"

usability_criteria:
  task_completion_rate: "> 90%"
  error_rate: "< 5%"
  user_satisfaction: "> 4.0/5.0"
  learning_curve: "< 30 minutes for new users"

accessibility_criteria:
  keyboard_navigation: "Full keyboard support"
  screen_reader: "Compatible with Arabic screen readers"
  color_contrast: "WCAG 2.1 AA compliant"
  touch_targets: "Minimum 44px touch targets"
```

### معايير التحسين
```yaml
optimization_targets:
  reduce_steps: "Minimize steps in critical flows"
  smart_defaults: "Use intelligent defaults"
  progressive_disclosure: "Show information progressively"
  contextual_help: "Provide help when needed"

arabic_ux_considerations:
  rtl_layout: "Proper right-to-left layout"
  arabic_typography: "Optimized Arabic text rendering"
  cultural_context: "Culturally appropriate interactions"
  local_patterns: "Follow local UX patterns"
```

---

## 📊 ملخص التدفقات (Flows Summary)

### إحصائيات التدفقات
```yaml
total_flows: 10
critical_flows: 4
average_steps: 8
estimated_completion_time: "2-5 minutes per flow"

complexity_distribution:
  simple: 3
  medium: 5
  complex: 2

platform_coverage:
  web: 10
  mobile: 8
  tablet: 10
```

### أولويات التطوير
```yaml
phase_1_flows:
  - "User Registration"
  - "Create Invoice"
  - "Customer Management"
  - "Secure Login"

phase_2_flows:
  - "Receipt Scanning"
  - "Reports & Analytics"
  - "Notifications"

phase_3_flows:
  - "Advanced Analytics"
  - "Error Handling"
  - "Dashboard Customization"
```

---

*هذه التدفقات تضمن تجربة مستخدم سلسة ومتسقة عبر جميع أجزاء المنصة، مع التركيز على الاحتياجات الخاصة للمستخدم العربي.*