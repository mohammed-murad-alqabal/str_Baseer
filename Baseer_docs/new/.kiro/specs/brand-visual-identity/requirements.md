# متطلبات نظام الهوية البصرية الموحدة

**المشروع:** بصير MVP - نظام إدارة الفواتير والعملاء  
**التاريخ:** 6 ديسمبر 2025  
**المؤلف:** فريق وكلاء تطوير مشروع بصير  
**الإصدار:** 2.0  
**الحالة:** مسودة محسّنة - جاهزة للمراجعة

---

## المقدمة

هذا المستند يحدد المتطلبات الشاملة والمحسّنة لتطبيق وتحسين نظام الهوية البصرية الموحدة لمشروع بصير. النظام مبني على وثيقة `BRAND_VISUAL_IDENTITY_SYSTEM.md` الشاملة ويهدف إلى تحقيق:

- **الاتساق البصري الكامل:** 100% التزام بمعايير الهوية عبر جميع المكونات
- **إمكانية الوصول الشاملة:** امتثال كامل لـ WCAG 2.1 Level AA (وتحقيق AAA حيثما أمكن)
- **الأداء العالي:** ≥ 60 FPS مع استجابة < 100ms
- **الجودة العالمية:** مطابقة لمعايير Material Design 3 و Apple HIG
- **القابلية للصيانة:** كود نظيف، موثق، وقابل للتوسع

### الأهداف الاستراتيجية

1. **الثقة المؤسسية:** تصميم يعكس احترافية مصرفية عالمية
2. **الوضوح المطلق:** لا غموض في أي عنصر بصري
3. **البساطة الوظيفية:** كل عنصر له هدف واضح
4. **الاستدامة:** نظام قابل للتطور مع نمو المشروع

## المصطلحات

- **النظام (System):** نظام الهوية البصرية الموحدة لمشروع بصير
- **المكون (Component):** أي عنصر UI قابل لإعادة الاستخدام (زر، بطاقة، حقل إدخال، إلخ)
- **الثيم (Theme):** مجموعة الألوان، الخطوط، والأنماط المطبقة على التطبيق
- **التباين (Contrast Ratio):** نسبة الفرق بين لونين محسوبة وفق معادلة WCAG (مثل 4.5:1)
- **WCAG:** Web Content Accessibility Guidelines - معايير إمكانية الوصول للمحتوى
- **APCA:** Advanced Perceptual Contrast Algorithm - خوارزمية التباين الإدراكي المتقدمة
- **Design Token:** قيمة تصميم ثابتة (لون، حجم خط، مسافة) مخزنة في متغير
- **Semantic Color:** لون له معنى وظيفي (primary، error، success)
- **Atomic Design:** منهجية تصميم تبدأ من أصغر عنصر (atom) إلى صفحة كاملة
- **المطور (Developer):** أي عضو في فريق التطوير يستخدم النظام
- **المستخدم النهائي (End User):** مستخدم تطبيق بصير
- **Screen Reader:** برنامج قراءة الشاشة للمستخدمين ضعاف البصر
- **Haptic Feedback:** ردود فعل لمسية (اهتزازات) عند التفاعل
- **Design System:** نظام شامل من المكونات، الأنماط، والإرشادات

---

## المتطلبات

### المتطلب 1: تطبيق المبادئ التصميمية الأساسية

**قصة المستخدم:** كمطور، أريد مبادئ تصميمية واضحة ومحددة، حتى أتمكن من اتخاذ قرارات تصميمية متسقة مع فلسفة المشروع.

#### معايير القبول

1. WHEN يقوم المطور بتصميم مكون جديد THEN النظام SHALL يطبق مبدأ "الأمان البصري" (ألوان هادئة، ظلال خفيفة، حواف ناعمة)
2. WHEN يتم عرض أي عنصر THEN النظام SHALL يطبق مبدأ "الوضوح المطلق" (لا غموض، كل عنصر واضح الغرض)
3. WHEN يتم إضافة ميزة جديدة THEN النظام SHALL يطبق مبدأ "البساطة الوظيفية" (كل عنصر له وظيفة، لا زخرفة بلا هدف)
4. WHEN يتم تطبيق نمط بصري THEN النظام SHALL يطبق مبدأ "الاتساق الصارم" (نفس العنصر يظهر بنفس الطريقة في كل مكان)
5. WHEN يتم تصميم واجهة THEN النظام SHALL يطبق مبدأ "إمكانية الوصول الشاملة" (التصميم للجميع بما في ذلك ذوي الاحتياجات الخاصة)
6. WHEN يتم تنفيذ تصميم THEN النظام SHALL يطبق مبدأ "الأداء البصري" (استخدام const، تقليل rebuilds، تحسين الأصول)

### المتطلب 2: تطبيق نظام الألوان الموحد والمتقدم

**قصة المستخدم:** كمطور، أريد نظام ألوان موحد، محدد بدقة علمية، ومتوافق مع المعايير العالمية، حتى أتمكن من بناء واجهات متسقة بصرياً، آمنة للوصول، وسهلة الصيانة على المدى الطويل.

#### معايير القبول

1. **نظام Design Tokens:**

   - WHEN يقوم المطور بإنشاء مكون جديد THEN النظام SHALL يوفر جميع الألوان من خلال `AppColors` class كـ design tokens ثابتة
   - WHEN يحتاج المطور للون THEN النظام SHALL يوفر ألوان semantic (primary، secondary، error، success، warning، info) وألوان neutral (background، surface، border)
   - WHEN يتم تعديل لون أساسي THEN النظام SHALL يحدّث جميع الألوان المشتقة تلقائياً

2. **امتثال WCAG 2.1 Level AA (إلزامي):**

   - WHEN يتم استخدام لون للنص العادي (< 18px) THEN النظام SHALL يضمن تباين ≥ 4.5:1 مع الخلفية
   - WHEN يتم استخدام لون للنص الكبير (≥ 18px أو ≥ 14px Bold) THEN النظام SHALL يضمن تباين ≥ 3:1 مع الخلفية
   - WHEN يتم استخدام لون لعنصر تفاعلي (زر، رابط، أيقونة) THEN النظام SHALL يضمن تباين ≥ 3:1 مع الخلفية المجاورة
   - WHEN يتم استخدام لون لرسم معلوماتي THEN النظام SHALL يضمن تباين ≥ 3:1 للعناصر الأساسية

3. **تحقيق WCAG 2.1 Level AAA (مستهدف):**

   - WHEN يتم استخدام لون للنص العادي THEN النظام SHOULD يحقق تباين ≥ 7:1 حيثما أمكن
   - WHEN يتم استخدام لون للنص الكبير THEN النظام SHOULD يحقق تباين ≥ 4.5:1 حيثما أمكن

4. **منع الألوان الصلبة (Hardcoded Colors):**

   - WHEN يقوم المطور بكتابة كود THEN النظام SHALL يمنع استخدام `Color(0xFF...)` مباشرة خارج `AppColors`
   - WHEN يتم فحص الكود (linting) THEN النظام SHALL يكتشف أي ألوان hardcoded ويرفض الـ commit
   - WHEN يحتاج المطور للون مخصص THEN النظام SHALL يوجهه لإضافته في `AppColors` أولاً

5. **الألوان الدلالية (Semantic Colors):**

   - WHEN يتم عرض حالة نجاح THEN النظام SHALL يستخدم `AppColors.success` (#2E7D32) مع أيقونة `check_circle_outline` ونص "نجح"
   - WHEN يتم عرض حالة خطأ THEN النظام SHALL يستخدم `AppColors.error` (#C62828) مع أيقونة `error_outline` ونص واضح للخطأ
   - WHEN يتم عرض حالة تحذير THEN النظام SHALL يستخدم `AppColors.warning` (#BF360C) مع أيقونة `warning_outlined` ونص التحذير
   - WHEN يتم عرض معلومة THEN النظام SHALL يستخدم `AppColors.info` (#0D47A1) مع أيقونة `info_outlined` ونص المعلومة
   - WHEN يتم عرض حالة THEN النظام SHALL يوفر 3 مؤشرات: لون + أيقونة + نص (لا يعتمد على اللون فقط)

6. **دعم الوضع الداكن (مستقبلي):**

   - WHEN يتم تفعيل الوضع الداكن THEN النظام SHALL يوفر لوحة ألوان داكنة متوافقة مع نفس معايير التباين
   - WHEN يتم التبديل بين الأوضاع THEN النظام SHALL يحافظ على نفس التسلسل الهرمي البصري

7. **اختبار الألوان تلقائياً:**

   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من تباين جميع أزواج الألوان المستخدمة
   - WHEN يفشل اختبار التباين THEN النظام SHALL يوفر تقرير مفصل بالألوان المخالفة والتباين الفعلي
   - WHEN يتم إضافة لون جديد THEN النظام SHALL يختبره تلقائياً مع جميع الخلفيات المحتملة

8. **توثيق الألوان:**
   - WHEN يفتح المطور دليل الألوان THEN النظام SHALL يعرض كل لون مع: الكود، الاسم، الاستخدام، التباين مع الخلفيات، أمثلة حية
   - WHEN يحتاج المطور لمعلومات عن لون THEN النظام SHALL يوفر السياق النفسي والثقافي للون

### المتطلب 2: تطبيق نظام الطباعة المتقدم والمحسّن معرفياً

**قصة المستخدم:** كمطور، أريد نظام طباعة موحد، محسّن معرفياً، ومبني على أبحاث علمية، حتى أتمكن من إنشاء تسلسل هرمي واضح، قراءة مريحة، وتجربة مستخدم متميزة.

#### معايير القبول

1. **نظام Type Scale المتقدم:**

   - WHEN يقوم المطور بإضافة نص THEN النظام SHALL يوفر 12 حجم خط محدد من خلال `AppTypography` class (displayLarge، displayMedium، displaySmall، headlineLarge، headlineMedium، headlineSmall، bodyLarge، bodyMedium، bodySmall، labelLarge، labelMedium، labelSmall)
   - WHEN يحتاج المطور لحجم خط THEN النظام SHALL يمنع استخدام أحجام خارج النظام المحدد
   - WHEN يتم تطبيق حجم خط THEN النظام SHALL يطبق تلقائياً: الوزن المناسب، lineHeight، letterSpacing

2. **خط Cairo - التحميل والأداء:**

   - WHEN يتم تشغيل التطبيق THEN النظام SHALL يحمّل خط Cairo محلياً من `assets/fonts/` (لا يحتاج إنترنت)
   - WHEN يتم عرض نص THEN النظام SHALL يستخدم خط Cairo حصرياً (لا fallback لخطوط أخرى)
   - WHEN يتم بناء التطبيق THEN النظام SHALL يتضمن 4 أوزان فقط: Regular (400)، Medium (500)، SemiBold (600)، Bold (700)
   - WHEN يتم تحميل الخط THEN النظام SHALL يستخدم font subsetting لتقليل الحجم (دعم العربية والإنجليزية فقط)

3. **التسلسل الهرمي البصري:**

   - WHEN يتم عرض عنوان رئيسي THEN النظام SHALL يستخدم displayLarge (34px، Bold، lineHeight: 1.2، letterSpacing: -0.5)
   - WHEN يتم عرض عنوان قسم THEN النظام SHALL يستخدم headlineLarge (22px، SemiBold، lineHeight: 1.2، letterSpacing: 0)
   - WHEN يتم عرض عنوان بطاقة THEN النظام SHALL يستخدم headlineMedium (18px، SemiBold، lineHeight: 1.2، letterSpacing: 0.15)
   - WHEN يتم عرض نص أساسي THEN النظام SHALL يستخدم bodyMedium (15px، Regular، lineHeight: 1.5، letterSpacing: 0.25)
   - WHEN يتم عرض تسمية زر THEN النظام SHALL يستخدم labelLarge (15px، Medium، lineHeight: 1.3، letterSpacing: 0.1)
   - WHEN يتم عرض نص صغير THEN النظام SHALL يستخدم bodySmall (13px، Regular، lineHeight: 1.5، letterSpacing: 0.4)

4. **التحسين المعرفي للقراءة:**

   - WHEN يتم عرض نص طويل (> 3 أسطر) THEN النظام SHALL يطبق lineHeight: 1.5 لتقليل الإجهاد البصري
   - WHEN يتم عرض عنوان THEN النظام SHALL يطبق lineHeight: 1.2 للتركيز والوضوح
   - WHEN يتم عرض نص عربي THEN النظام SHALL يطبق letterSpacing: 0.25-0.5 لتحسين القراءة
   - WHEN يتم عرض عنوان كبير THEN النظام SHALL يطبق letterSpacing سالب (-0.5 إلى 0) لتحسين التماسك البصري

5. **دعم Text Scaling الشامل:**

   - WHEN يقوم المستخدم بتغيير حجم النص (accessibility settings) THEN النظام SHALL يدعم scaling من 0.8x إلى 2.0x
   - WHEN يتم تطبيق text scaling THEN النظام SHALL يمنع overflow باستخدام `ResponsiveText` widget
   - WHEN يصل scaling إلى 1.5x+ THEN النظام SHALL يقلل letterSpacing تلقائياً لتجنب التباعد المفرط
   - WHEN يتم عرض نص مع scaling THEN النظام SHALL يحافظ على التسلسل الهرمي النسبي

6. **دعم RTL الكامل:**

   - WHEN يتم عرض نص عربي THEN النظام SHALL يطبق TextDirection.rtl تلقائياً
   - WHEN يتم محاذاة نص THEN النظام SHALL يستخدم TextAlign.start/end (لا left/right)
   - WHEN يتم عرض أرقام THEN النظام SHALL يستخدم الأرقام الهندية (123) بشكل متسق
   - WHEN يتم عرض علامات ترقيم THEN النظام SHALL يستخدم العلامات العربية (،؟؛)

7. **الأداء والتحسين:**

   - WHEN يتم عرض نص ثابت THEN النظام SHALL يستخدم `const Text()` لتقليل rebuilds
   - WHEN يتم عرض نص متكرر THEN النظام SHALL يستخدم `TextStyle` مخزن مؤقتاً
   - WHEN يتم قياس الأداء THEN النظام SHALL يحقق < 16ms لرسم النصوص (60 FPS)

8. **اختبار الطباعة:**
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من استخدام خط Cairo فقط
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من استخدام أحجام من `AppTypography` فقط
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من عدم وجود overflow في جميع أحجام scaling
   - WHEN يتم إضافة نص جديد THEN النظام SHALL يختبر القراءة مع مستخدمين حقيقيين (user testing)

### المتطلب 3: تطبيق نظام المسافات والتخطيط المتقدم (8-Point Grid System)

**قصة المستخدم:** كمطور، أريد نظام مسافات موحد ومبني على 8-point grid، حتى أتمكن من إنشاء تخطيطات متسقة، منظمة، ومتناسقة رياضياً عبر جميع الشاشات والأجهزة.

#### معايير القبول

1. **نظام 8-Point Grid الصارم:**

   - WHEN يقوم المطور بإضافة padding أو margin THEN النظام SHALL يوفر قيم المسافات من خلال `AppSpacing` class حصرياً
   - WHEN يتم تطبيق مسافة THEN النظام SHALL يستخدم مضاعفات 4px فقط: xs (4px)، sm (8px)، md (16px)، lg (24px)، xl (32px)، xxl (48px)
   - WHEN يحتاج المطور لمسافة مخصصة THEN النظام SHALL يرفض أي قيمة ليست مضاعف 4
   - WHEN يتم فحص الكود THEN النظام SHALL يكتشف أي مسافات hardcoded ويرفض الـ commit

2. **التطبيق السياقي للمسافات:**

   - WHEN يتم إضافة padding داخلي لمكون THEN النظام SHALL يستخدم md (16px) كافتراضي
   - WHEN يتم إضافة margin بين عناصر THEN النظام SHALL يستخدم sm (8px) كافتراضي
   - WHEN يتم إضافة margin بين أقسام THEN النظام SHALL يستخدم lg (24px) كافتراضي
   - WHEN يتم إضافة margin بين مجموعات THEN النظام SHALL يستخدم xl (32px) كافتراضي
   - WHEN يتم إضافة فاصل رئيسي THEN النظام SHALL يستخدم xxl (48px)

3. **نظام Border Radius الموحد:**

   - WHEN يتم إنشاء بطاقة (Card) THEN النظام SHALL يطبق borderRadius: 12px (md)
   - WHEN يتم إنشاء زر (Button) THEN النظام SHALL يطبق borderRadius: 12px (md)
   - WHEN يتم إنشاء حقل إدخال (TextField) THEN النظام SHALL يطبق borderRadius: 12px (md)
   - WHEN يتم إنشاء حوار (Dialog) THEN النظام SHALL يطبق borderRadius: 16px (lg)
   - WHEN يتم إنشاء ورقة سفلية (Bottom Sheet) THEN النظام SHALL يطبق borderRadius: 16px (lg) للأعلى فقط
   - WHEN يتم إنشاء زر دائري THEN النظام SHALL يطبق borderRadius: 999px (full)

4. **نظام Elevation والظلال:**

   - WHEN يتم إنشاء بطاقة عادية THEN النظام SHALL يستخدم elevation: 0 مع border: 1px solid AppColors.border
   - WHEN يتم إنشاء FAB (Floating Action Button) THEN النظام SHALL يستخدم elevation: 4
   - WHEN يتم إنشاء حوار أو ورقة سفلية THEN النظام SHALL يستخدم elevation: 8
   - WHEN يتم إنشاء قائمة منسدلة THEN النظام SHALL يستخدم elevation: 8
   - WHEN يتم استخدام ظل THEN النظام SHALL يستخدم shadowColor: #1A000000 (أسود شفاف 10%)

5. **التخطيط المتجاوب (Responsive Layout):**

   - WHEN يتم عرض المحتوى على شاشة صغيرة (< 375px) THEN النظام SHALL يقلل المسافات بنسبة 25%
   - WHEN يتم عرض المحتوى على شاشة كبيرة (> 768px) THEN النظام SHALL يزيد المسافات بنسبة 25%
   - WHEN يتم عرض المحتوى على جهاز لوحي THEN النظام SHALL يستخدم تخطيط two-pane حيثما أمكن
   - WHEN يتم تغيير حجم الشاشة THEN النظام SHALL يحافظ على نسب المسافات

6. **Safe Area والحواف:**

   - WHEN يتم عرض محتوى THEN النظام SHALL يحترم safe area (notch، home indicator)
   - WHEN يتم عرض محتوى قرب الحواف THEN النظام SHALL يطبق padding: 16px على الأقل
   - WHEN يتم عرض محتوى في landscape THEN النظام SHALL يزيد horizontal padding

7. **اختبار المسافات:**

   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من استخدام قيم من `AppSpacing` فقط
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من borderRadius المحدد لكل نوع مكون
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من عدم وجود تداخل بين العناصر

8. **التوثيق البصري:**
   - WHEN يفتح المطور دليل المسافات THEN النظام SHALL يعرض grid overlay تفاعلي
   - WHEN يفتح المطور دليل المسافات THEN النظام SHALL يعرض أمثلة حية لكل قيمة مسافة

### المتطلب 4: تطبيق نظام الأيقونات الموحد

**قصة المستخدم:** كمطور، أريد نظام أيقونات موحد (Material Icons Outlined)، حتى أتمكن من إنشاء واجهات نظيفة ومتسقة.

#### معايير القبول

1. WHEN يقوم المطور بإضافة أيقونة THEN النظام SHALL يستخدم Material Icons Outlined فقط
2. WHEN يتم عرض أيقونة THEN النظام SHALL يطبق الحجم المناسب من `AppIconSize` (18، 22، 26، 32، 40، 48px)
3. WHEN يتم عرض أيقونة نشطة THEN النظام SHALL يستخدم `AppColors.primary` (#0056B3)
4. WHEN يتم عرض أيقونة غير نشطة THEN النظام SHALL يستخدم `AppColors.textHint` (#616161)
5. WHEN يتم عرض أيقونة THEN النظام SHALL يضمن تباين ≥ 3:1 مع الخلفية

### المتطلب 5: ضمان إمكانية الوصول (Accessibility)

**قصة المستخدم:** كمستخدم نهائي ذو احتياجات خاصة، أريد تطبيقاً يدعم إمكانية الوصول بالكامل، حتى أتمكن من استخدامه بسهولة.

#### معايير القبول

1. WHEN يتم عرض أي نص THEN النظام SHALL يضمن تباين ≥ 4.5:1 مع الخلفية (WCAG AA)
2. WHEN يتم عرض عنصر تفاعلي THEN النظام SHALL يضمن حجم ≥ 44x44px
3. WHEN يقوم المستخدم بتغيير حجم النص THEN النظام SHALL يدعم text scaling حتى 200% بدون overflow
4. WHEN يتم عرض معلومة بالألوان THEN النظام SHALL يوفر مؤشرات بديلة (أيقونات + نصوص)
5. WHEN يستخدم المستخدم screen reader THEN النظام SHALL يوفر semantic labels لجميع العناصر التفاعلية

### المتطلب 6: إنشاء اختبارات آلية للهوية البصرية

**قصة المستخدم:** كمطور، أريد اختبارات آلية تتحقق من الالتزام بمعايير الهوية البصرية، حتى أتمكن من اكتشاف الانحرافات مبكراً.

#### معايير القبول

1. WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من تباين جميع الألوان المستخدمة
2. WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من استخدام خط Cairo فقط
3. WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من استخدام أحجام خطوط من `AppTypography`
4. WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من استخدام مسافات من `AppSpacing`
5. WHEN يتم تشغيل الاختبارات THEN النظام SHALL يتحقق من حجم العناصر التفاعلية ≥ 44x44px

### المتطلب 7: إنشاء دليل استخدام تفاعلي

**قصة المستخدم:** كمطور جديد، أريد دليل استخدام تفاعلي للهوية البصرية، حتى أتمكن من فهم وتطبيق النظام بسرعة.

#### معايير القبول

1. WHEN يفتح المطور الدليل THEN النظام SHALL يعرض جميع الألوان مع أمثلة حية
2. WHEN يفتح المطور الدليل THEN النظام SHALL يعرض جميع أحجام الخطوط مع أمثلة حية
3. WHEN يفتح المطور الدليل THEN النظام SHALL يعرض جميع المكونات الأساسية (أزرار، بطاقات، حقول) مع كود قابل للنسخ
4. WHEN يفتح المطور الدليل THEN النظام SHALL يوفر أمثلة على حالات الاستخدام الشائعة
5. WHEN يفتح المطور الدليل THEN النظام SHALL يوفر أداة لاختبار التباين بين الألوان

### المتطلب 8: إنشاء نظام إدارة الأصول البصرية

**قصة المستخدم:** كمطور، أريد نظام منظم لإدارة الأصول البصرية (خطوط، أيقونات، صور)، حتى أتمكن من الوصول إليها بسهولة.

#### معايير القبول

1. WHEN يحتاج المطور لخط THEN النظام SHALL يوفر جميع أوزان Cairo (400، 500، 600، 700) في `assets/fonts/`
2. WHEN يحتاج المطور لأيقونة THEN النظام SHALL يوفر دليل بجميع الأيقونات المستخدمة مع أسمائها
3. WHEN يحتاج المطور لصورة THEN النظام SHALL يوفر بنية منظمة في `assets/images/`
4. WHEN يضيف المطور أصل جديد THEN النظام SHALL يوفر إرشادات واضحة للتسمية والتنظيم
5. WHEN يتم بناء التطبيق THEN النظام SHALL يحسّن جميع الأصول تلقائياً (ضغط، تحسين)

### المتطلب 9: تطبيق نظام الحركة والتفاعل

**قصة المستخدم:** كمستخدم نهائي، أريد حركات سلسة ومتسقة، حتى أشعر بالاحترافية والراحة أثناء الاستخدام.

#### معايير القبول

1. WHEN يتم الانتقال بين الشاشات THEN النظام SHALL يطبق حركة slide + fade بمدة 300ms
2. WHEN يظهر عنصر جديد THEN النظام SHALL يطبق حركة fadeIn + scaleUp بمدة 200ms
3. WHEN يختفي عنصر THEN النظام SHALL يطبق حركة fadeOut + scaleDown بمدة 200ms
4. WHEN يضغط المستخدم على زر THEN النظام SHALL يوفر haptic feedback + scale animation بمدة 100ms
5. WHEN يتم تحميل بيانات THEN النظام SHALL يعرض CircularProgressIndicator بلون `AppColors.primary`

### المتطلب 10: إنشاء نظام مراقبة الالتزام والجودة المستمرة

**قصة المستخدم:** كمدير مشروع، أريد نظام مراقبة شامل يتتبع الالتزام بمعايير الهوية البصرية في الوقت الفعلي، حتى أتمكن من ضمان الجودة المستمرة، اكتشاف الانحرافات مبكراً، وقياس التحسن بمرور الوقت.

#### معايير القبول

1. **المراقبة التلقائية في CI/CD:**

   - WHEN يتم commit كود جديد THEN النظام SHALL يشغل اختبارات الهوية البصرية تلقائياً في GitHub Actions
   - WHEN يتم فتح PR THEN النظام SHALL يعرض تقرير مفصل بالالتزام: نسبة النجاح، المخالفات، التوصيات
   - WHEN يتم اكتشاف انحراف حرج (تباين < 4.5:1، حجم عنصر < 44px) THEN النظام SHALL يمنع merge ويطلب إصلاح فوري
   - WHEN يتم اكتشاف انحراف غير حرج THEN النظام SHALL يسمح بـ merge مع تحذير ويضيف issue تلقائياً

2. **Dashboard الشامل:**

   - WHEN يطلب المدير تقرير THEN النظام SHALL يوفر dashboard تفاعلي يعرض:
     - نسبة الالتزام الإجمالية (%)
     - نسبة الالتزام لكل فئة (ألوان، طباعة، مسافات، أيقونات، إمكانية وصول)
     - نسبة الالتزام لكل مكون (AppButton: 95%، AppCard: 98%، إلخ)
     - تطور الالتزام بمرور الوقت (رسم بياني)
     - أكثر المخالفات شيوعاً
     - المكونات التي تحتاج تحسين
   - WHEN يتم عرض dashboard THEN النظام SHALL يحدّث البيانات في الوقت الفعلي

3. **التوصيات الذكية:**

   - WHEN يتم اكتشاف مخالفة THEN النظام SHALL يوفر:
     - وصف واضح للمشكلة
     - الكود المخالف (مع رقم السطر)
     - الحل المقترح (كود صحيح)
     - رابط للوثائق ذات الصلة
     - أمثلة مشابهة تم إصلاحها سابقاً
   - WHEN يتكرر نوع مخالفة THEN النظام SHALL يقترح إضافة lint rule جديد

4. **التنبيهات والإشعارات:**

   - WHEN تنخفض نسبة الالتزام < 90% THEN النظام SHALL يرسل تنبيه للفريق
   - WHEN يتم اكتشاف مخالفة حرجة THEN النظام SHALL يرسل إشعار فوري للمطور المسؤول
   - WHEN يتم إصلاح مخالفة THEN النظام SHALL يرسل تأكيد بالإصلاح

5. **التقارير الدورية:**
   - WHEN ينتهي الأسبوع THEN النظام SHALL يولد تقرير أسبوعي بالإنجازات والتحديات
   - WHEN ينتهي الشهر THEN النظام SHALL يولد تقرير شهري شامل مع توصيات استراتيجية
   - WHEN يتم طلب تقرير مخصص THEN النظام SHALL يوفر أداة لتوليد تقارير حسب الفترة والفئة

### المتطلب 11: تطبيق نظام الأداء والتحسين

**قصة المستخدم:** كمطور، أريد نظام يضمن أداء عالي (≥ 60 FPS) واستجابة سريعة (< 100ms)، حتى يشعر المستخدم بالسلاسة والاحترافية.

#### معايير القبول

1. **معايير الأداء الإلزامية:**

   - WHEN يتم عرض أي شاشة THEN النظام SHALL يحقق معدل إطارات ≥ 60 FPS
   - WHEN يتفاعل المستخدم مع عنصر THEN النظام SHALL يستجيب خلال < 100ms
   - WHEN يتم تحميل شاشة THEN النظام SHALL يعرض المحتوى خلال < 1.5 ثانية
   - WHEN يتم تشغيل حركة THEN النظام SHALL يحافظ على 60 FPS طوال الحركة

2. **تحسين الرسم (Rendering):**

   - WHEN يتم عرض نص ثابت THEN النظام SHALL يستخدم `const Text()` لتجنب rebuilds
   - WHEN يتم عرض قائمة طويلة THEN النظام SHALL يستخدم `ListView.builder` مع lazy loading
   - WHEN يتم عرض صورة THEN النظام SHALL يستخدم caching وتحميل تدريجي
   - WHEN يتم عرض مكون معقد THEN النظام SHALL يستخدم `RepaintBoundary` لعزل الرسم

3. **تحسين الذاكرة:**

   - WHEN يتم استخدام الألوان THEN النظام SHALL يخزنها كـ static const لتجنب إنشاء instances جديدة
   - WHEN يتم استخدام TextStyle THEN النظام SHALL يخزنها مؤقتاً ويعيد استخدامها
   - WHEN يتم عرض قائمة THEN النظام SHALL يحرر العناصر غير المرئية من الذاكرة

4. **قياس الأداء:**
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يقيس وقت رسم كل مكون
   - WHEN يتجاوز مكون 16ms للرسم THEN النظام SHALL يصدر تحذير
   - WHEN يتم بناء التطبيق THEN النظام SHALL يولد تقرير أداء شامل

### المتطلب 12: تطبيق نظام الاختبار البصري المتقدم

**قصة المستخدم:** كمطور، أريد اختبارات بصرية شاملة تكتشف أي تغيير غير مقصود في الواجهة، حتى أتمكن من الحفاظ على الاتساق البصري.

#### معايير القبول

1. **Snapshot Testing:**

   - WHEN يتم إنشاء مكون جديد THEN النظام SHALL يلتقط snapshot بصري له
   - WHEN يتم تعديل مكون THEN النظام SHALL يقارن الـ snapshot الجديد بالقديم
   - WHEN يختلف الـ snapshot THEN النظام SHALL يطلب مراجعة يدوية للتأكيد
   - WHEN يتم الموافقة على التغيير THEN النظام SHALL يحدّث الـ snapshot المرجعي

2. **Visual Regression Testing:**

   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يلتقط screenshots لجميع الشاشات الرئيسية
   - WHEN يتم مقارنة screenshots THEN النظام SHALL يكتشف اختلافات بدقة pixel-perfect
   - WHEN يتم اكتشاف اختلاف THEN النظام SHALL يعرض diff بصري واضح

3. **اختبار إمكانية الوصول:**
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يفحص جميع العناصر التفاعلية للتأكد من حجم ≥ 44x44px
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يفحص جميع النصوص للتأكد من تباين ≥ 4.5:1
   - WHEN يتم تشغيل الاختبارات THEN النظام SHALL يفحص جميع العناصر للتأكد من semantic labels

### المتطلب 13: تطبيق نظام التوثيق التفاعلي والحي

**قصة المستخدم:** كمطور جديد، أريد توثيق تفاعلي وحي يساعدني على فهم وتطبيق النظام بسرعة، حتى أتمكن من الإنتاجية الفورية.

#### معايير القبول

1. **Storybook/Component Gallery:**

   - WHEN يفتح المطور الدليل THEN النظام SHALL يعرض جميع المكونات في gallery تفاعلي
   - WHEN يختار المطور مكون THEN النظام SHALL يعرض:
     - المكون الحي (يمكن التفاعل معه)
     - جميع الحالات (default، hover، pressed، disabled، error)
     - جميع الأحجام (small، medium، large)
     - الكود الكامل (قابل للنسخ)
     - Props والمعاملات
     - أمثلة استخدام شائعة
   - WHEN يعدّل المطور props THEN النظام SHALL يحدّث المكون فوراً

2. **Design Tokens Viewer:**

   - WHEN يفتح المطور دليل الألوان THEN النظام SHALL يعرض:
     - جميع الألوان مع hex codes
     - التباين مع كل خلفية
     - أمثلة استخدام حية
     - السياق النفسي والثقافي
   - WHEN يفتح المطور دليل الخطوط THEN النظام SHALL يعرض جميع الأحجام مع أمثلة حية

3. **أداة اختبار التباين:**
   - WHEN يفتح المطور أداة التباين THEN النظام SHALL يوفر:
     - Color picker لاختيار لونين
     - حساب تلقائي للتباين
     - تقييم WCAG (AA، AAA)
     - اقتراحات لتحسين التباين
     - أمثلة بصرية

### المتطلب 14: تطبيق نظام الإصدارات والتطور

**قصة المستخدم:** كمدير منتج، أريد نظام واضح لإدارة إصدارات الهوية البصرية، حتى أتمكن من تطوير النظام بشكل منظم دون كسر التوافق.

#### معايير القبول

1. **Semantic Versioning:**

   - WHEN يتم تغيير لون أساسي THEN النظام SHALL يزيد major version (1.0.0 → 2.0.0)
   - WHEN يتم إضافة لون جديد THEN النظام SHALL يزيد minor version (1.0.0 → 1.1.0)
   - WHEN يتم إصلاح bug THEN النظام SHALL يزيد patch version (1.0.0 → 1.0.1)

2. **Changelog:**

   - WHEN يتم إصدار نسخة جديدة THEN النظام SHALL يوثق جميع التغييرات في CHANGELOG.md
   - WHEN يتم تغيير breaking THEN النظام SHALL يوفر migration guide

3. **Deprecation Policy:**
   - WHEN يتم إيقاف لون أو مكون THEN النظام SHALL يعلن عن deprecation قبل 3 أشهر
   - WHEN يتم استخدام عنصر deprecated THEN النظام SHALL يعرض تحذير واضح مع البديل

---

## الأولويات

| المتطلب | الأولوية  | المبرر                        |
| :------ | :-------- | :---------------------------- |
| 1       | 🔴 عالية  | أساسي لجميع المكونات          |
| 2       | 🔴 عالية  | أساسي للقراءة والتسلسل الهرمي |
| 3       | 🔴 عالية  | أساسي للتخطيط المتسق          |
| 4       | 🟡 متوسطة | مهم للاتساق البصري            |
| 5       | 🔴 عالية  | إلزامي للامتثال WCAG          |
| 6       | 🟡 متوسطة | مهم للجودة المستمرة           |
| 7       | 🟢 منخفضة | مفيد للمطورين الجدد           |
| 8       | 🟡 متوسطة | مهم للتنظيم والصيانة          |
| 9       | 🟡 متوسطة | مهم لتجربة المستخدم           |
| 10      | 🟢 منخفضة | مفيد للمراقبة طويلة المدى     |

---

## التبعيات

- **Flutter SDK:** 3.24.0+
- **Dart:** 3.5.0+
- **خط Cairo:** يجب أن يكون موجوداً في `assets/fonts/`
- **Material Icons:** مدمج في Flutter
- **نظام الاختبارات:** يجب أن يكون مكتملاً (المرحلة 3)

---

## القيود

- يجب الالتزام بـ WCAG 2.1 Level AA
- يجب دعم RTL بالكامل
- يجب دعم text scaling حتى 200%
- يجب أن يعمل على Android 5.0+ و iOS 12.0+
- يجب أن يكون الأداء ≥ 60 FPS

---

**تم إعداده بواسطة:** فريق وكلاء تطوير مشروع بصير  
**التاريخ:** 6 ديسمبر 2025  
**الإصدار:** 1.0  
**الحالة:** ✅ جاهز للمراجعة

---

## الأولويات والجدول الزمني

### المرحلة 1: الأساسيات الحرجة (أسبوع 1-2)

| المتطلب | الأولوية | المدة المتوقعة | المبرر                                  |
| :------ | :------- | :------------- | :-------------------------------------- |
| 1       | 🔴 حرجة  | 3 أيام         | أساسي لجميع المكونات - يجب تطبيقه أولاً |
| 2       | 🔴 حرجة  | 3 أيام         | أساسي للقراءة والتسلسل الهرمي           |
| 3       | 🔴 حرجة  | 2 أيام         | أساسي للتخطيط المتسق                    |
| 5       | 🔴 حرجة  | 2 أيام         | إلزامي للامتثال WCAG - متطلب قانوني     |

### المرحلة 2: التحسينات الأساسية (أسبوع 3-4)

| المتطلب | الأولوية | المدة المتوقعة | المبرر                                       |
| :------ | :------- | :------------- | :------------------------------------------- |
| 4       | 🟡 عالية | 2 أيام         | مهم للاتساق البصري                           |
| 6       | 🟡 عالية | 3 أيام         | مهم للجودة المستمرة والاكتشاف المبكر للمشاكل |
| 9       | 🟡 عالية | 2 أيام         | مهم لتجربة المستخدم والشعور بالاحترافية      |
| 11      | 🟡 عالية | 2 أيام         | مهم للأداء وتجربة المستخدم                   |

### المرحلة 3: الأدوات والتوثيق (أسبوع 5-6)

| المتطلب | الأولوية  | المدة المتوقعة | المبرر                             |
| :------ | :-------- | :------------- | :--------------------------------- |
| 7       | 🟢 متوسطة | 3 أيام         | مفيد للمطورين الجدد وتسريع التطوير |
| 8       | 🟢 متوسطة | 2 أيام         | مهم للتنظيم والصيانة طويلة المدى   |
| 13      | 🟢 متوسطة | 3 أيام         | مفيد للتوثيق والتدريب              |

### المرحلة 4: المراقبة والتطور (أسبوع 7-8)

| المتطلب | الأولوية  | المدة المتوقعة | المبرر                                          |
| :------ | :-------- | :------------- | :---------------------------------------------- |
| 10      | 🟢 متوسطة | 3 أيام         | مفيد للمراقبة طويلة المدى وضمان الجودة المستمرة |
| 12      | 🟢 متوسطة | 3 أيام         | مفيد للاختبار البصري والحفاظ على الاتساق        |
| 14      | 🟢 منخفضة | 2 أيام         | مفيد لإدارة التطور المستقبلي                    |

**إجمالي المدة المتوقعة:** 8 أسابيع (40 يوم عمل)

---

## التبعيات التقنية

### متطلبات البيئة

- **Flutter SDK:** 3.24.0+ (مع دعم Material 3)
- **Dart:** 3.5.0+ (مع null safety)
- **Android SDK:** API 21+ (Android 5.0 Lollipop)
- **iOS SDK:** 12.0+ (iOS 12)
- **Web:** Chrome 90+، Firefox 88+، Safari 14+

### الأصول المطلوبة

- **خط Cairo:** 4 أوزان (Regular 400، Medium 500، SemiBold 600، Bold 700) في `assets/fonts/`
- **Material Icons:** مدمج في Flutter (لا يحتاج تثبيت)
- **الشعار:** ملفات SVG و PNG بأحجام متعددة في `assets/icons/`

### المكتبات المطلوبة

```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0 # للتنسيق والترجمة

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 # للفحص الصارم
  golden_toolkit: ^0.15.0 # لـ snapshot testing
```

### التبعيات الداخلية

- **نظام الاختبارات:** يجب أن يكون مكتملاً (المرحلة 3) قبل البدء بالمتطلب 6
- **CI/CD Pipeline:** يجب أن يكون جاهزاً قبل البدء بالمتطلب 10
- **Documentation System:** يجب أن يكون جاهزاً قبل البدء بالمتطلب 13

---

## القيود والمعايير

### القيود التقنية

1. **الأداء:**

   - معدل الإطارات: ≥ 60 FPS (16.67ms لكل إطار)
   - وقت الاستجابة: < 100ms للتفاعلات
   - وقت التحميل: < 1.5 ثانية للشاشات
   - استهلاك الذاكرة: < 150 MB للتطبيق

2. **التوافق:**

   - Android: API 21+ (95% من الأجهزة)
   - iOS: 12.0+ (98% من الأجهزة)
   - Web: متصفحات حديثة (آخر سنتين)
   - RTL: دعم كامل للعربية

3. **الحجم:**
   - حجم التطبيق: < 50 MB (release build)
   - حجم الخطوط: < 2 MB (بعد subsetting)
   - حجم الأصول: < 5 MB إجمالي

### المعايير الإلزامية

1. **إمكانية الوصول (WCAG 2.1):**

   - **Level AA (إلزامي):**
     - تباين النصوص: ≥ 4.5:1 (عادي)، ≥ 3:1 (كبير)
     - حجم العناصر التفاعلية: ≥ 44x44px
     - دعم keyboard navigation
     - دعم screen readers
   - **Level AAA (مستهدف):**
     - تباين النصوص: ≥ 7:1 (عادي)، ≥ 4.5:1 (كبير)
     - دعم text spacing
     - دعم reflow حتى 400%

2. **Material Design 3:**

   - التزام بـ Material Design 3 guidelines
   - استخدام Material Components
   - تطبيق Material Motion
   - دعم Dynamic Color (مستقبلي)

3. **Apple Human Interface Guidelines:**
   - التزام بـ HIG على iOS
   - استخدام native patterns
   - دعم Dark Mode (مستقبلي)
   - دعم Dynamic Type

### معايير الجودة

1. **الكود:**

   - Test Coverage: ≥ 70%
   - Documentation Coverage: ≥ 95%
   - Linting: 0 errors، 0 warnings
   - Code Review: إلزامي لجميع التغييرات

2. **التصميم:**

   - Brand Compliance: 100%
   - Accessibility Compliance: 100% (WCAG AA)
   - Performance: 100% (60 FPS)
   - Consistency: 100% (جميع المكونات متسقة)

3. **التوثيق:**
   - جميع المكونات موثقة
   - جميع الألوان موثقة مع أمثلة
   - جميع الخطوط موثقة مع أمثلة
   - دليل استخدام شامل

---

## مقاييس النجاح (Success Metrics)

### مقاييس تقنية

| المقياس                    | الهدف   | طريقة القياس                      |
| :------------------------- | :------ | :-------------------------------- |
| **Brand Compliance**       | 100%    | اختبارات آلية + مراجعة يدوية      |
| **WCAG AA Compliance**     | 100%    | أدوات فحص التباين + اختبارات آلية |
| **Test Coverage**          | ≥ 70%   | flutter test --coverage           |
| **Performance (FPS)**      | ≥ 60    | Flutter DevTools                  |
| **Response Time**          | < 100ms | قياس يدوي + اختبارات              |
| **Build Size**             | < 50MB  | flutter build --release           |
| **Documentation Coverage** | ≥ 95%   | مراجعة يدوية                      |

### مقاييس تجربة المستخدم

| المقياس                  | الهدف     | طريقة القياس                |
| :----------------------- | :-------- | :-------------------------- |
| **User Satisfaction**    | ≥ 4.5/5   | استبيانات + تقييمات         |
| **Task Completion Rate** | ≥ 95%     | user testing                |
| **Error Rate**           | < 5%      | تتبع الأخطاء                |
| **Learning Curve**       | < 5 دقائق | user testing للمطورين الجدد |

### مقاييس الصيانة

| المقياس                    | الهدف     | طريقة القياس          |
| :------------------------- | :-------- | :-------------------- |
| **Time to Fix Bug**        | < 2 ساعات | تتبع issues           |
| **Time to Add Component**  | < 4 ساعات | تتبع development time |
| **Code Reusability**       | ≥ 80%     | تحليل الكود           |
| **Developer Satisfaction** | ≥ 4/5     | استبيانات داخلية      |

---

## المخاطر والتخفيف

### المخاطر المحتملة

1. **مخاطر تقنية:**

   - **الخطر:** صعوبة تحقيق 60 FPS على أجهزة قديمة
   - **التخفيف:** تحسين الأداء، استخدام const constructors، profiling مستمر
   - **الاحتمالية:** متوسطة | **التأثير:** عالي

2. **مخاطر الجودة:**

   - **الخطر:** عدم الالتزام بالمعايير من قبل المطورين
   - **التخفيف:** اختبارات آلية، lint rules صارمة، مراجعة كود إلزامية
   - **الاحتمالية:** متوسطة | **التأثير:** عالي

3. **مخاطر الجدول الزمني:**

   - **الخطر:** تأخر في التطبيق بسبب تعقيد المتطلبات
   - **التخفيف:** تقسيم إلى مراحل، تحديد أولويات واضحة، مراجعة أسبوعية
   - **الاحتمالية:** منخفضة | **التأثير:** متوسط

4. **مخاطر الموارد:**
   - **الخطر:** نقص المطورين المتخصصين في UI/UX
   - **التخفيف:** توثيق شامل، تدريب، دليل استخدام تفاعلي
   - **الاحتمالية:** منخفضة | **التأثير:** متوسط

---

## الخلاصة

هذا المستند يحدد **14 متطلباً شاملاً** لتطبيق وتحسين نظام الهوية البصرية الموحدة لمشروع بصير. المتطلبات مبنية على:

- ✅ **معايير عالمية:** WCAG 2.1، Material Design 3، Apple HIG
- ✅ **أبحاث علمية:** علم النفس المعرفي، نظرية الجشطالت، دراسات القراءة
- ✅ **أفضل الممارسات:** Design Systems، Atomic Design، Design Tokens
- ✅ **قابلية القياس:** مقاييس واضحة ومحددة لكل متطلب
- ✅ **الاستدامة:** نظام قابل للتطور والصيانة على المدى الطويل

### الأهداف الرئيسية

1. **الاتساق:** 100% التزام بمعايير الهوية عبر جميع المكونات
2. **إمكانية الوصول:** 100% امتثال لـ WCAG 2.1 Level AA
3. **الأداء:** ≥ 60 FPS مع استجابة < 100ms
4. **الجودة:** ≥ 70% test coverage، 0 linting errors
5. **الصيانة:** توثيق شامل، أدوات تفاعلية، مراقبة مستمرة

### الخطوات التالية

1. **مراجعة المتطلبات:** مراجعة شاملة من الفريق والموافقة
2. **إنشاء التصميم:** تحويل المتطلبات إلى تصميم تقني مفصل
3. **تخطيط المهام:** تقسيم التصميم إلى مهام قابلة للتنفيذ
4. **البدء بالتطبيق:** تنفيذ المرحلة 1 (الأساسيات الحرجة)

---

**تم إعداده بواسطة:** فريق وكلاء تطوير مشروع بصير  
**التاريخ:** 6 ديسمبر 2025  
**الإصدار:** 2.0  
**الحالة:** ✅ جاهز للمراجعة والموافقة

---

## ملحق: مراجع ومصادر

### معايير ومواصفات

- [WCAG 2.1 Guidelines](https://www.w3.org/WAI/WCAG21/quickref/)
- [Material Design 3](https://m3.material.io/)
- [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- [Flutter Accessibility](https://docs.flutter.dev/development/accessibility-and-localization/accessibility)

### أبحاث علمية

- "The Psychology of Color in Marketing" - Journal of Marketing Research
- "Typography and Readability" - MIT Media Lab
- "Gestalt Principles in UI Design" - Nielsen Norman Group
- "Cognitive Load Theory" - John Sweller

### أدوات وموارد

- [Contrast Checker](https://webaim.org/resources/contrastchecker/)
- [Color Blind Simulator](https://www.color-blindness.com/coblis-color-blindness-simulator/)
- [Flutter DevTools](https://docs.flutter.dev/tools/devtools)
- [Golden Toolkit](https://pub.dev/packages/golden_toolkit)
