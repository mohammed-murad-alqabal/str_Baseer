# Git Hooks للمشروع

**المشروع:** بصير MVP  
**التاريخ:** 9 ديسمبر 2025  
**الحالة:** ✅ نشط

---

## 🎯 الهدف

توفير hooks تلقائية للحفاظ على جودة وتنظيم المشروع.

---

## 📋 الـ Hooks المتاحة

### 1. pre-commit-file-check

**الغرض:** منع إضافة ملفات غير مرغوبة في جذر المشروع

**يتحقق من:**

- ملفات `.log` في الجذر
- ملفات `.tmp`, `.temp`, `.bak`
- ملفات `.db`, `.so`, `.dll`
- تقارير بصيغة `*_REPORT.md` في الجذر
- ملفات `CHECKPOINT_*.md`, `CONTINUATION_*.md`

**الاستخدام:**

```bash
# يعمل تلقائياً عند git commit
git commit -m "message"
```

**إذا فشل:**

```bash
# انقل الملفات إلى أماكنها الصحيحة
mv *.log logs/
mv *_REPORT.md .kiro/docs/reports/

# ثم حاول مرة أخرى
git commit -m "message"
```

---

## 🔧 التفعيل

### الطريقة 1: تلقائي (موصى به)

```bash
# تفعيل جميع الـ hooks
bash scripts/install_hooks.sh
```

### الطريقة 2: يدوي

```bash
# ربط الـ hooks
git config core.hooksPath .githooks

# أو نسخها
cp .githooks/* .git/hooks/
chmod +x .git/hooks/*
```

---

## ✅ التحقق من التفعيل

```bash
# التحقق من الإعدادات
git config core.hooksPath

# يجب أن يظهر: .githooks
```

---

## 🧪 الاختبار

### اختبار pre-commit-file-check

```bash
# إنشاء ملف اختبار
echo "test" > test.log

# محاولة commit
git add test.log
git commit -m "test"

# يجب أن يفشل مع رسالة واضحة
# ثم احذف الملف
rm test.log
```

---

## 📝 إضافة hook جديد

1. إنشاء الملف في `.githooks/`
2. جعله قابل للتنفيذ: `chmod +x .githooks/hook-name`
3. توثيقه هنا
4. اختباره

---

## 🔗 المراجع

- **معايير التنظيم:** `.kiro/standards/file-organization.md`
- **Git Guide:** `.kiro/guides/git-guide.md`

---

**تم إعداده بواسطة:** فريق وكلاء تطوير مشروع بصير  
**التاريخ:** 9 ديسمبر 2025
