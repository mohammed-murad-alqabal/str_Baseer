# 🚀 دليل تشغيل بصير MVP - Baseer MVP Deployment Guide

## 📋 نظرة عامة

بصير MVP هو نظام إدارة الفواتير الإلكترونية المتوافق مع زاتكا، مصمم خصيصاً للسوق العربية. هذا الدليل يوضح كيفية تشغيل التطبيق محلياً أو نشره في بيئة الإنتاج.

## ✅ متطلبات النظام

### البرمجيات المطلوبة

- **Flutter SDK**: 3.35.5 أو أحدث
- **Dart SDK**: 3.9.2 أو أحدث
- **Android Studio**: للتطوير على Android
- **VS Code**: محرر النصوص المفضل
- **Git**: لإدارة الكود المصدري

### متطلبات النظام

- **نظام التشغيل**: Windows 10+, macOS 10.14+, أو Linux
- **الذاكرة**: 8 GB RAM كحد أدنى (16 GB مفضل)
- **مساحة القرص**: 10 GB مساحة فارغة
- **الاتصال**: اتصال إنترنت مستقر

## 🛠️ التثبيت والإعداد

### 1. تحضير البيئة

```bash
# تحقق من Flutter
flutter doctor

# تحديث Flutter إلى أحدث إصدار
flutter upgrade

# تفعيل منصات الويب والسطح المكتبي
flutter config --enable-web
flutter config --enable-linux-desktop
flutter config --enable-windows-desktop
flutter config --enable-macos-desktop
```

### 2. تحميل المشروع

```bash
# استنساخ المشروع
git clone <repository-url>
cd baseer-mvp

# تحميل التبعيات
flutter pub get
```

### 3. إعداد قاعدة البيانات

```bash
# إنشاء قاعدة البيانات المحلية
# سيتم إنشاؤها تلقائياً عند أول تشغيل
```

## 🚀 تشغيل التطبيق

### تشغيل على الويب (مفضل للاختبار)

```bash
# تشغيل في وضع التطوير
flutter run -d chrome

# أو بناء للإنتاج
flutter build web --release
```

### تشغيل على Android

```bash
# تأكد من توصيل جهاز أو تشغيل محاكي
flutter devices

# تشغيل التطبيق
flutter run -d android
```

### تشغيل على سطح المكتب

```bash
# Linux
flutter run -d linux

# Windows
flutter run -d windows

# macOS
flutter run -d macos
```

## 🔧 الإعدادات

### متغيرات البيئة

إنشاء ملف `.env` في جذر المشروع:

```env
# إعدادات قاعدة البيانات
DATABASE_URL=sqlite:baseer_local.db

# إعدادات زاتكا (للإنتاج)
ZATCA_SANDBOX_URL=https://gw-fatoora.zatca.gov.sa/e-invoicing/developer-portal
ZATCA_PRODUCTION_URL=https://gw-fatoora.zatca.gov.sa/e-invoicing/core

# إعدادات الأمان
JWT_SECRET=your-secret-key-here
ENCRYPTION_KEY=your-encryption-key-here

# إعدادات الدفع (اختيارية)
MOYASAR_API_KEY=your-moyasar-key
PAYTABS_API_KEY=your-paytabs-key
```

### إعدادات التطبيق

يمكن تخصيص الإعدادات في `lib/core/constants/app_constants.dart`:

```dart
// تغيير البيئة
EnvironmentConfig.currentEnvironment = Environment.production;

// تخصيص الألوان والخطوط
// راجع lib/core/theme/ للمزيد من الخيارات
```

## 📱 الميزات المتاحة

### ✅ الميزات المكتملة

1. **نظام المصادقة**

   - تسجيل الدخول والخروج
   - إنشاء حسابات جديدة
   - المصادقة الثنائية (MFA)
   - إدارة الجلسات

2. **إدارة العملاء**

   - إضافة وتعديل العملاء
   - البحث والتصفية
   - تصنيف العملاء (أفراد/شركات)

3. **إدارة الفواتير**

   - إنشاء فواتير عادية ومبسطة
   - حساب الضريبة تلقائياً (15%)
   - إنتاج ملفات PDF
   - تتبع حالة الفواتير

4. **التوافق مع زاتكا**

   - التحقق من صحة الفواتير
   - إنتاج رموز QR
   - إرسال للزاتكا (جاهز للتفعيل)

5. **التقارير والإحصائيات**

   - لوحة تحكم تفاعلية
   - تقارير المبيعات
   - تقارير الضرائب
   - تصدير البيانات

6. **الدعم العربي الكامل**

   - واجهة باللغة العربية
   - دعم RTL
   - خطوط عربية محسنة
   - تنسيق التواريخ والأرقام

7. **الأمان والامتثال**
   - تشفير البيانات
   - سجل المراجعة
   - النسخ الاحتياطي
   - امتثال PDPL

## 🔍 اختبار التطبيق

### اختبار الوظائف الأساسية

1. **تسجيل الدخول**

   ```
   البريد الإلكتروني: admin@baseer.sa
   كلمة المرور: Admin123!
   ```

2. **إنشاء عميل جديد**

   - اذهب إلى "العملاء" → "إضافة عميل"
   - أدخل البيانات المطلوبة
   - احفظ العميل

3. **إنشاء فاتورة**

   - اذهب إلى "الفواتير" → "فاتورة جديدة"
   - اختر العميل
   - أضف البنود
   - احفظ الفاتورة

4. **عرض التقارير**
   - اذهب إلى "التقارير"
   - اختر نوع التقرير
   - حدد الفترة الزمنية

### اختبار الأداء

```bash
# اختبار الأداء
flutter test test/performance_test.dart

# اختبار التكامل
flutter test integration_test/
```

## 🚀 النشر في الإنتاج

### نشر على الويب

```bash
# بناء للإنتاج
flutter build web --release

# نسخ الملفات إلى الخادم
cp -r build/web/* /var/www/baseer/

# إعداد Nginx (مثال)
server {
    listen 80;
    server_name baseer.sa;
    root /var/www/baseer;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### نشر على Android

```bash
# بناء APK
flutter build apk --release

# أو بناء App Bundle للمتجر
flutter build appbundle --release
```

### نشر باستخدام Docker

```dockerfile
FROM nginx:alpine
COPY build/web /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

```bash
# بناء الصورة
docker build -t baseer-mvp .

# تشغيل الحاوية
docker run -p 80:80 baseer-mvp
```

## 🔧 استكشاف الأخطاء

### مشاكل شائعة وحلولها

1. **خطأ في التبعيات**

   ```bash
   flutter clean
   flutter pub get
   ```

2. **مشاكل في البناء**

   ```bash
   flutter doctor
   flutter upgrade
   ```

3. **مشاكل في قاعدة البيانات**

   - احذف ملف `baseer_local.db`
   - أعد تشغيل التطبيق

4. **مشاكل في الخطوط العربية**
   - تأكد من وجود ملفات الخطوط في `assets/fonts/`
   - تحقق من `pubspec.yaml`

### سجلات الأخطاء

```bash
# عرض السجلات
flutter logs

# أو في وضع التطوير
flutter run --verbose
```

## 📞 الدعم والمساعدة

### الموارد المفيدة

- **الوثائق**: راجع ملفات `docs/` في المشروع
- **أمثلة الكود**: راجع مجلد `examples/`
- **اختبارات**: راجع مجلد `test/`

### معلومات الاتصال

- **البريد الإلكتروني**: support@baseer.sa
- **الموقع**: https://baseer.sa
- **الوثائق**: https://docs.baseer.sa

## 📄 الترخيص

هذا المشروع محمي بحقوق الطبع والنشر. راجع ملف `LICENSE` للمزيد من التفاصيل.

---

## 🎯 الخطوات التالية

بعد التشغيل الناجح، يمكنك:

1. **تخصيص الواجهة**: تعديل الألوان والخطوط
2. **إضافة ميزات جديدة**: راجع خارطة الطريق
3. **التكامل مع الأنظمة الخارجية**: APIs إضافية
4. **تحسين الأداء**: مراقبة ومتابعة الاستخدام

**مبروك! 🎉 تطبيق بصير MVP جاهز للاستخدام!**
