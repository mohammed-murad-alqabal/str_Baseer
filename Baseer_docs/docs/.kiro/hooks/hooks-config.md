# Agent Hooks Configuration - Baseer Project

## 📋 نظرة عامة على الـ Hooks

تم تكوين **10 Agent Hooks** متخصصة لدعم تطوير منظومة بصير بشكل متكامل وتلقائي.

## 🔧 الـ Hooks المُفعلة

### 1. **Auto-Formatter** ✅
- **الهدف**: تنسيق تلقائي لملفات Dart و Go
- **المحفزات**: `**/*.dart`, `**/*.go`
- **الأدوات**: `dart format`, `go fmt`

### 2. **Static Code Analyzer** ✅
- **الهدف**: تحليل ثابت للكود وكشف المشاكل
- **المحفزات**: `**/*.dart`
- **الأدوات**: `flutter analyze`

### 3. **Barrel File Generator** ✅
- **الهدف**: إنشاء ملفات barrel للتصدير المنظم
- **المحفزات**: `lib/features/**/*.dart`, `lib/shared/**/*.dart`
- **الوظائف**: تصدير تلقائي، ترتيب أبجدي

### 4. **Feature Scaffold Generator** ✅
- **الهدف**: إنشاء هيكل Clean Architecture للميزات الجديدة
- **المحفزات**: `lib/features/*/`
- **البنية**: data/domain/presentation layers

### 5. **Arabic Content Validator** 🆕
- **الهدف**: التحقق من المحتوى العربي ودعم RTL
- **المحفزات**: `assets/translations/*.json`, `**/*_ar.dart`, `**/rtl_*.dart`
- **التحقق**: ترميز عربي، اتجاه النص، المفاتيح

### 6. **Go Backend Validator** 🆕
- **الهدف**: التحقق من كود Go والتبعيات
- **المحفزات**: `backend/**/*.go`, `backend/go.mod`
- **الأدوات**: `go vet`, `golint`, `gosec`

### 7. **ZATCA Compliance Checker** 🆕
- **الهدف**: التحقق من متطلبات الفوترة الإلكترونية
- **المحفزات**: `**/zatca*.dart`, `**/invoice*.dart`, `**/tax*.dart`
- **التحقق**: QR codes، التوقيع الرقمي، ضريبة القيمة المضافة

### 8. **Security & Privacy Validator** 🆕
- **الهدف**: التحقق من الأمان وحماية البيانات
- **المحفزات**: `**/*auth*.dart`, `**/*security*.dart`, `**/.env*`
- **التحقق**: التشفير، JWT، البيانات الحساسة

### 9. **Performance Monitor** 🆕
- **الهدف**: مراقبة الأداء والاستعلامات
- **المحفزات**: `**/database*.dart`, `**/api*.dart`, `**/widget*.dart`
- **الأهداف**: API < 200ms، تحميل الصفحة < 2s

### 10. **Test Automation** 🆕
- **الهدف**: تشغيل الاختبارات التلقائية
- **المحفزات**: `lib/**/*.dart`, `backend/**/*.go`, `test/**/*.dart`
- **الاختبارات**: Unit، Integration، Widget tests

## 🎯 مصفوفة التكامل

| Hook | Flutter | Go Backend | ZATCA | Arabic | Security | Performance |
|------|---------|------------|-------|--------|----------|-------------|
| Auto-Formatter | ✅ | ✅ | - | - | - | - |
| Static Analyzer | ✅ | - | - | - | - | - |
| Barrel Generator | ✅ | - | - | - | - | - |
| Feature Scaffold | ✅ | - | - | - | - | - |
| Arabic Validator | ✅ | - | - | ✅ | - | - |
| Go Validator | - | ✅ | - | - | ✅ | ✅ |
| ZATCA Checker | ✅ | ✅ | ✅ | - | ✅ | - |
| Security Validator | ✅ | ✅ | - | - | ✅ | - |
| Performance Monitor | ✅ | ✅ | - | - | - | ✅ |
| Test Automation | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

## 🚀 أوامر التشغيل والإدارة

### تفعيل/إلغاء تفعيل Hooks
```bash
# عرض جميع الـ hooks
ls -la .kiro/hooks/

# التحقق من صحة JSON
find .kiro/hooks -name "*.kiro.hook" -exec jq . {} \;

# تعديل حالة التفعيل
jq '.enabled = false' .kiro/hooks/hook-name.kiro.hook > temp.json && mv temp.json .kiro/hooks/hook-name.kiro.hook
```

### مراقبة الأداء
```bash
# Flutter
flutter analyze
flutter test
flutter build --release

# Go Backend
cd backend
go vet ./...
go test ./...
go mod tidy
```

### اختبار التكامل
```bash
# إنشاء ملف اختبار لتفعيل الـ hooks
touch lib/features/test_feature/test_file.dart
touch backend/internal/test/test_service.go
touch assets/translations/test_ar.json
```

## 📊 مؤشرات الأداء المستهدفة

### Frontend (Flutter)
- **تحميل الصفحة**: < 2 ثانية
- **استجابة UI**: < 16ms (60 FPS)
- **حجم التطبيق**: < 50MB
- **استهلاك الذاكرة**: < 200MB

### Backend (Go)
- **استجابة API**: < 200ms (95th percentile)
- **معدل الخطأ**: < 0.1%
- **التوفر**: 99.99%
- **استهلاك CPU**: < 70%

### ZATCA Compliance
- **نجاح الإرسال**: > 99%
- **وقت التوقيع**: < 500ms
- **صحة QR Code**: 100%
- **التحقق من الشهادات**: < 1s

## 🔒 الأمان والخصوصية

### البيانات المحمية
- أرقام الهوية الوطنية
- معلومات البطاقات الائتمانية
- مفاتيح API وشهادات ZATCA
- كلمات المرور وتوكنات JWT

### معايير التشفير
- **البيانات المخزنة**: AES-256
- **النقل**: TLS 1.3
- **التوقيع الرقمي**: RSA-2048/ECDSA
- **Hashing**: SHA-256

## 🌍 دعم اللغة العربية

### المتطلبات
- **الترميز**: UTF-8
- **اتجاه النص**: RTL
- **الخطوط**: Noto Sans Arabic
- **التوطين**: ar_SA, en_US

### التحقق التلقائي
- صحة ملفات الترجمة JSON
- اتساق المفاتيح بين اللغات
- اتجاه النص في الواجهات
- السياق الثقافي للمحتوى

## 📈 التحسين المستمر

### مراجعة دورية
- **أسبوعية**: مراجعة أداء الـ hooks
- **شهرية**: تحديث القواعد والمعايير
- **ربع سنوية**: تقييم شامل للتكامل

### التطوير المستقبلي
- إضافة hooks للـ CI/CD
- دعم المزيد من اللغات
- تحسين الذكاء الاصطناعي
- تكامل مع أدوات المراقبة

---

**آخر تحديث**: 15 أكتوبر 2025  
**الإصدار**: 1.0.0  
**المطور**: فريق بصير التقني