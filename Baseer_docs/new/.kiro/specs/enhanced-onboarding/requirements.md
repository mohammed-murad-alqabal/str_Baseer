# متطلبات نظام Onboarding المحسّن

**المشروع:** بصير MVP  
**التاريخ:** 7 ديسمبر 2025  
**المؤلف:** فريق وكلاء تطوير مشروع بصير  
**الحالة:** 🔄 قيد المراجعة

---

## المقدمة

نظام Onboarding المحسّن هو تجربة الاستخدام الأولى للمستخدم الجديد في تطبيق بصير. الهدف هو تحقيق "First Success" في أقل من 5 دقائق، مع ضمان فهم المستخدم للقيمة الأساسية للتطبيق وتمكينه من إنشاء أول فاتورة بسهولة وسرعة.

### الأهداف الاستراتيجية

1. **تقليل Time to First Invoice** إلى أقل من 5 دقائق
2. **تحقيق Activation Rate** أكبر من 80%
3. **تحسين Retention Day 7** إلى أكثر من 40%
4. **بناء الثقة** من اللحظة الأولى

### المقاييس المستهدفة

| المقياس                    |   الهدف   | الحالي |
| :------------------------- | :-------: | :----: |
| Time to First Invoice      | < 5 دقائق |   -    |
| Onboarding Completion Rate |   > 80%   |   -    |
| User Satisfaction (NPS)    |   > 50    |   -    |
| Retention Day 7            |   > 40%   |   -    |

---

## المصطلحات (Glossary)

- **النظام (System)**: تطبيق بصير MVP
- **المستخدم (User)**: صاحب عمل صغير أو مستقل يستخدم التطبيق لأول مرة
- **Onboarding**: عملية تعريف المستخدم الجديد بالتطبيق وميزاته
- **First Success**: إنشاء أول فاتورة بنجاح
- **Welcome Experience**: الشاشة الترحيبية الأولى
- **Quick Setup**: إعداد سريع لمعلومات الشركة
- **Guided Tour**: جولة إرشادية اختيارية
- **قالب الفاتورة (Invoice Template)**: تصميم جاهز للفاتورة

---

## المتطلبات

### المتطلب 1: Welcome Experience (تجربة الترحيب)

**User Story:** كمستخدم جديد، أريد أن أفهم قيمة التطبيق بسرعة، حتى أقرر الاستمرار في استخدامه.

#### معايير القبول

1. WHEN المستخدم يفتح التطبيق لأول مرة THEN النظام SHALL يعرض شاشة ترحيب جذابة تحتوي على شعار التطبيق واسمه
2. WHEN المستخدم يشاهد شاشة الترحيب THEN النظام SHALL يعرض فيديو ترحيبي قصير (30 ثانية) يشرح القيمة الأساسية للتطبيق
3. WHEN المستخدم ينتهي من مشاهدة الفيديو أو يتخطاه THEN النظام SHALL ينتقل تلقائياً إلى شاشة اختيار نوع العمل
4. WHEN المستخدم يضغط على زر "تخطي" THEN النظام SHALL ينتقل فوراً إلى شاشة اختيار نوع العمل
5. WHEN المستخدم يشاهد الفيديو THEN النظام SHALL يعرض مؤشر تقدم واضح يوضح مدة الفيديو المتبقية

### المتطلب 2: اختيار نوع العمل

**User Story:** كمستخدم جديد، أريد اختيار نوع عملي، حتى يتم تخصيص التطبيق لاحتياجاتي الخاصة.

#### معايير القبول

1. WHEN المستخدم يصل إلى شاشة اختيار نوع العمل THEN النظام SHALL يعرض أربعة خيارات واضحة: مستقل، محل، شركة، مهني
2. WHEN المستخدم يختار نوع العمل THEN النظام SHALL يحفظ الاختيار في قاعدة البيانات المحلية
3. WHEN المستخدم يختار نوع العمل THEN النظام SHALL يعرض قوالب فواتير مناسبة لنوع العمل المختار
4. WHEN المستخدم يختار "مستقل" THEN النظام SHALL يعرض قوالب بسيطة ومباشرة
5. WHEN المستخدم يختار "شركة" THEN النظام SHALL يعرض قوالب احترافية ومفصلة

### المتطلب 3: اختيار قالب الفاتورة

**User Story:** كمستخدم جديد، أريد اختيار قالب فاتورة يناسب عملي، حتى تبدو فواتيري احترافية من البداية.

#### معايير القبول

1. WHEN المستخدم يصل إلى شاشة اختيار القالب THEN النظام SHALL يعرض 3 قوالب أساسية مناسبة لنوع العمل المختار
2. WHEN المستخدم يضغط على قالب THEN النظام SHALL يعرض معاينة كاملة للقالب مع بيانات تجريبية
3. WHEN المستخدم يختار قالب THEN النظام SHALL يحفظ القالب كافتراضي للمستخدم
4. WHEN المستخدم يعاين القالب THEN النظام SHALL يعرض جميع عناصر القالب بوضوح (الشعار، العنوان، البنود، الضريبة، الإجمالي)
5. WHEN المستخدم يختار قالب THEN النظام SHALL ينتقل تلقائياً إلى شاشة Quick Setup

### المتطلب 4: Quick Setup (الإعداد السريع)

**User Story:** كمستخدم جديد، أريد إدخال معلومات شركتي الأساسية بسرعة، حتى أبدأ في إنشاء الفواتير فوراً.

#### معايير القبول

1. WHEN المستخدم يصل إلى شاشة Quick Setup THEN النظام SHALL يعرض نموذج بسيط يحتوي على 4 حقول فقط: اسم الشركة، رقم الهاتف، العنوان، البريد الإلكتروني (اختياري)
2. WHEN المستخدم يدخل اسم الشركة THEN النظام SHALL يتحقق من أن الاسم غير فارغ ويحتوي على حرفين على الأقل
3. WHEN المستخدم يدخل رقم الهاتف THEN النظام SHALL يتحقق من أن الرقم يبدأ بـ 05 ويتكون من 10 أرقام
4. WHEN المستخدم يدخل البريد الإلكتروني THEN النظام SHALL يتحقق من صحة تنسيق البريد الإلكتروني
5. WHEN المستخدم يكمل النموذج ويضغط "التالي" THEN النظام SHALL يحفظ المعلومات في قاعدة البيانات المحلية
6. WHEN المستخدم يضغط "التالي" بدون إدخال البريد الإلكتروني THEN النظام SHALL يسمح بالمتابعة (الحقل اختياري)
7. WHEN المستخدم يكمل Quick Setup THEN النظام SHALL ينتقل إلى شاشة First Success

### المتطلب 5: First Success (النجاح الأول)

**User Story:** كمستخدم جديد، أريد إنشاء أول فاتورة بسهولة، حتى أشعر بالإنجاز وأفهم قيمة التطبيق.

#### معايير القبول

1. WHEN المستخدم يصل إلى شاشة First Success THEN النظام SHALL يعرض دليل خطوة بخطوة لإنشاء أول عميل
2. WHEN المستخدم يضغط "إضافة عميل" THEN النظام SHALL يفتح نموذج مبسط مع بيانات تجريبية مملوءة مسبقاً كمثال
3. WHEN المستخدم يحفظ العميل THEN النظام SHALL يعرض رسالة تهنئة ويوجه المستخدم لإنشاء أول فاتورة
4. WHEN المستخدم يضغط "إنشاء فاتورة" THEN النظام SHALL يفتح شاشة إنشاء فاتورة مع العميل المُنشأ محدد مسبقاً
5. WHEN المستخدم يضيف بند واحد على الأقل ويحفظ الفاتورة THEN النظام SHALL يعرض رسالة احتفالية "تهانينا! أنشأت أول فاتورة"
6. WHEN المستخدم ينشئ أول فاتورة THEN النظام SHALL يعرض خيارات: تصدير PDF، إنشاء فاتورة جديدة، أو بدء الجولة الإرشادية
7. WHEN المستخدم يصدر أول PDF THEN النظام SHALL يعرض رسالة نجاح مع أيقونة احتفالية

### المتطلب 6: Guided Tour (الجولة الإرشادية)

**User Story:** كمستخدم جديد، أريد جولة إرشادية اختيارية، حتى أتعرف على جميع ميزات التطبيق بشكل تفاعلي.

#### معايير القبول

1. WHEN المستخدم يكمل First Success THEN النظام SHALL يعرض خيار "بدء الجولة الإرشادية" أو "تخطي"
2. WHEN المستخدم يختار "بدء الجولة" THEN النظام SHALL يعرض جولة تفاعلية تشرح الميزات الأساسية (لوحة التحكم، العملاء، الفواتير، الإعدادات)
3. WHEN المستخدم يشاهد الجولة THEN النظام SHALL يعرض مؤشر تقدم يوضح الخطوة الحالية من إجمالي الخطوات
4. WHEN المستخدم يضغط "تخطي" في أي وقت THEN النظام SHALL ينتقل فوراً إلى لوحة التحكم الرئيسية
5. WHEN المستخدم ينهي الجولة THEN النظام SHALL يحفظ حالة "تم إكمال Onboarding" ولا يعرض الجولة مرة أخرى
6. WHEN المستخدم يريد إعادة الجولة لاحقاً THEN النظام SHALL يوفر خيار "إعادة الجولة الإرشادية" في الإعدادات

### المتطلب 7: نصائح Contextual

**User Story:** كمستخدم جديد، أريد نصائح مفيدة في الوقت المناسب، حتى أتعلم استخدام التطبيق بشكل طبيعي.

#### معايير القبول

1. WHEN المستخدم يفتح شاشة جديدة لأول مرة THEN النظام SHALL يعرض tooltip صغير يشرح الغرض من الشاشة
2. WHEN المستخدم يمرر الماوس (أو يضغط) على عنصر معقد THEN النظام SHALL يعرض نصيحة سريعة توضح وظيفة العنصر
3. WHEN المستخدم يرى نصيحة THEN النظام SHALL يعرض خيار "فهمت" أو "لا تظهر مرة أخرى"
4. WHEN المستخدم يختار "لا تظهر مرة أخرى" THEN النظام SHALL يحفظ التفضيل ولا يعرض هذه النصيحة مجدداً
5. WHEN المستخدم يكمل إجراء معين THEN النظام SHALL يعرض نصيحة للخطوة التالية المقترحة

### المتطلب 8: مركز المساعدة المدمج

**User Story:** كمستخدم جديد، أريد الوصول السريع للمساعدة، حتى أحل أي مشكلة أو استفسار فوراً.

#### معايير القبول

1. WHEN المستخدم يضغط على أيقونة المساعدة (؟) THEN النظام SHALL يفتح مركز مساعدة مدمج يحتوي على الأسئلة الشائعة
2. WHEN المستخدم يبحث في مركز المساعدة THEN النظام SHALL يعرض نتائج البحث مرتبة حسب الصلة
3. WHEN المستخدم يفتح مقال مساعدة THEN النظام SHALL يعرض المحتوى بتنسيق واضح مع صور توضيحية
4. WHEN المستخدم لا يجد إجابة THEN النظام SHALL يعرض خيار "تواصل معنا" يفتح نموذج دعم
5. WHEN المستخدم في شاشة معينة ويضغط المساعدة THEN النظام SHALL يعرض مساعدة contextual خاصة بتلك الشاشة

### المتطلب 9: حفظ التقدم

**User Story:** كمستخدم جديد، أريد أن يحفظ التطبيق تقدمي، حتى أستطيع إكمال Onboarding لاحقاً إذا توقفت.

#### معايير القبول

1. WHEN المستخدم يغلق التطبيق أثناء Onboarding THEN النظام SHALL يحفظ الخطوة الحالية في قاعدة البيانات المحلية
2. WHEN المستخدم يفتح التطبيق مرة أخرى THEN النظام SHALL يستأنف Onboarding من آخر خطوة محفوظة
3. WHEN المستخدم يكمل Onboarding THEN النظام SHALL يحذف بيانات التقدم ويحفظ حالة "مكتمل"
4. WHEN المستخدم يريد إعادة Onboarding THEN النظام SHALL يوفر خيار "إعادة تعيين Onboarding" في الإعدادات
5. WHEN المستخدم يعيد تعيين Onboarding THEN النظام SHALL يحذف جميع بيانات التقدم ويبدأ من البداية

### المتطلب 10: التخصيص حسب نوع العمل

**User Story:** كمستخدم، أريد تجربة مخصصة حسب نوع عملي، حتى أشعر أن التطبيق مصمم خصيصاً لي.

#### معايير القبول

1. WHEN المستخدم يختار "مستقل" THEN النظام SHALL يعرض نصائح وأمثلة خاصة بالمستقلين
2. WHEN المستخدم يختار "محل" THEN النظام SHALL يعرض قوالب بسيطة ومباشرة مناسبة للمحلات
3. WHEN المستخدم يختار "شركة" THEN النظام SHALL يعرض ميزات متقدمة مثل إدارة الفريق والتقارير
4. WHEN المستخدم يختار "مهني" THEN النظام SHALL يعرض قوالب احترافية مناسبة للأطباء والمحامين
5. WHEN المستخدم يكمل Onboarding THEN النظام SHALL يحفظ نوع العمل ويستخدمه لتخصيص التجربة المستقبلية

---

## المتطلبات غير الوظيفية

### الأداء

1. WHEN المستخدم ينتقل بين خطوات Onboarding THEN النظام SHALL يستجيب في أقل من 500 ميلي ثانية
2. WHEN المستخدم يشاهد الفيديو الترحيبي THEN النظام SHALL يحمل الفيديو في أقل من 2 ثانية
3. WHEN المستخدم يحفظ البيانات THEN النظام SHALL يكمل العملية في أقل من 1 ثانية

### سهولة الاستخدام

1. WHEN المستخدم يستخدم Onboarding THEN النظام SHALL يضمن أن جميع النصوص واضحة ومقروءة بخط لا يقل عن 14px
2. WHEN المستخدم يضغط على أي زر THEN النظام SHALL يوفر feedback بصري فوري (تغيير لون، رسوم متحركة)
3. WHEN المستخدم يرتكب خطأ THEN النظام SHALL يعرض رسالة خطأ واضحة ومفيدة باللغة العربية

### إمكانية الوصول

1. WHEN المستخدم يستخدم قارئ الشاشة THEN النظام SHALL يوفر نصوص بديلة لجميع العناصر المرئية
2. WHEN المستخدم يستخدم التطبيق THEN النظام SHALL يضمن تباين ألوان لا يقل عن 4.5:1 (WCAG AA)
3. WHEN المستخدم يستخدم لوحة المفاتيح THEN النظام SHALL يدعم التنقل الكامل بلوحة المفاتيح

### الأمان

1. WHEN المستخدم يدخل بيانات شخصية THEN النظام SHALL يحفظ البيانات محلياً فقط بدون إرسالها لأي خادم
2. WHEN المستخدم يكمل Onboarding THEN النظام SHALL يشفر جميع البيانات الحساسة في قاعدة البيانات المحلية

---

## الخلاصة

نظام Onboarding المحسّن هو المفتاح لتحقيق Product-Market Fit في Q1 2026. من خلال تجربة سلسة وسريعة ومخصصة، نضمن أن المستخدمين الجدد يفهمون قيمة التطبيق ويحققون "First Success" في أقل من 5 دقائق.

### الأولويات

1. **عالية جداً**: المتطلبات 1-5 (Welcome → First Success)
2. **عالية**: المتطلبات 6-7 (Guided Tour + Contextual Tips)
3. **متوسطة**: المتطلبات 8-10 (Help Center + Progress + Customization)

### التبعيات

- تصميم UI/UX للشاشات
- إنتاج فيديو ترحيبي (30 ثانية)
- إنشاء محتوى مركز المساعدة
- تصميم 3 قوالب فواتير أساسية لكل نوع عمل

---

**تم إعداده بواسطة:** فريق وكلاء تطوير مشروع بصير  
**التاريخ:** 7 ديسمبر 2025  
**الإصدار:** 1.0  
**الحالة:** ✅ معتمد
