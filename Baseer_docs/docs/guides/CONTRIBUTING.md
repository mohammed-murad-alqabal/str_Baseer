# 🤝 دليل المساهمة في مشروع Baseer

مرحباً بك في مشروع **Baseer**! نحن نقدر اهتمامك بالمساهمة في تطوير نظام التشغيل المالي الذكي للمنطقة العربية.

## 📋 جدول المحتويات

- [مبادئ المساهمة](#مبادئ-المساهمة)
- [كيفية المساهمة](#كيفية-المساهمة)
- [معايير الكود](#معايير-الكود)
- [عملية المراجعة](#عملية-المراجعة)
- [الإبلاغ عن المشاكل](#الإبلاغ-عن-المشاكل)
- [طلب ميزات جديدة](#طلب-ميزات-جديدة)
- [إرشادات التطوير](#إرشادات-التطوير)

## 🎯 مبادئ المساهمة

### قيمنا الأساسية
- **الجودة أولاً**: نسعى للحصول على كود عالي الجودة ومُختبر جيداً
- **التعاون**: نؤمن بقوة العمل الجماعي والتعاون البناء
- **الشمولية**: نرحب بجميع المساهمين بغض النظر عن خلفيتهم
- **التعلم المستمر**: نشجع التعلم وتبادل المعرفة

### مدونة السلوك
- احترام جميع المساهمين والمستخدمين
- استخدام لغة مهذبة وبناءة
- قبول النقد البناء بصدر رحب
- التركيز على ما هو أفضل للمجتمع

## 🚀 كيفية المساهمة

### 1. إعداد بيئة التطوير

```bash
# استنساخ المشروع
git clone https://github.com/baseer-team/baseer.git
cd baseer

# إعداد Backend
cd backend
go mod download
cp .env.example .env

# إعداد Frontend
cd ../frontend
flutter pub get

# تشغيل البيئة المحلية
docker-compose up -d
```

### 2. اختيار مهمة للعمل عليها

- تصفح [Issues المفتوحة](https://github.com/baseer-team/baseer/issues)
- ابحث عن Issues مع تسمية `good first issue` للمبتدئين
- اقرأ الوصف والمتطلبات بعناية
- علق على Issue لإعلام الفريق برغبتك في العمل عليها

### 3. إنشاء فرع جديد

```bash
# إنشاء فرع من main
git checkout main
git pull origin main
git checkout -b feature/your-feature-name

# أو للإصلاحات
git checkout -b fix/issue-description
```

### 4. تطوير الميزة أو الإصلاح

- اتبع [معايير الكود](#معايير-الكود)
- اكتب اختبارات للكود الجديد
- تأكد من نجاح جميع الاختبارات الموجودة
- حدث الوثائق عند الحاجة

### 5. إرسال Pull Request

```bash
# إضافة التغييرات
git add .
git commit -m "feat: add new feature description"

# رفع الفرع
git push origin feature/your-feature-name
```

ثم افتح Pull Request على GitHub مع:
- وصف واضح للتغييرات
- ربط Issue ذات الصلة
- لقطات شاشة إذا كانت التغييرات تؤثر على UI

## 📝 معايير الكود

### Backend (Go)

```go
// استخدم أسماء واضحة ومعبرة
func CreateInvoice(ctx context.Context, req CreateInvoiceRequest) (*Invoice, error) {
    // التحقق من صحة البيانات
    if err := req.Validate(); err != nil {
        return nil, fmt.Errorf("invalid request: %w", err)
    }
    
    // منطق الأعمال
    invoice := &Invoice{
        ID:     uuid.New(),
        UserID: req.UserID,
        // ...
    }
    
    return invoice, nil
}
```

**إرشادات Go:**
- استخدم `gofmt` لتنسيق الكود
- اتبع إرشادات [Effective Go](https://golang.org/doc/effective_go.html)
- اكتب تعليقات للدوال المُصدرة
- استخدم معالجة الأخطاء المناسبة
- اكتب اختبارات وحدة شاملة

### Frontend (Flutter)

```dart
// استخدم أسماء واضحة للمتغيرات والدوال
class InvoiceCard extends ConsumerWidget {
  const InvoiceCard({
    super.key,
    required this.invoice,
    this.onTap,
  });

  final Invoice invoice;
  final VoidCallback? onTap;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final theme = Theme.of(context);
    
    return Card(
      child: ListTile(
        title: Text(
          invoice.invoiceNumber,
          style: theme.textTheme.titleMedium,
        ),
        subtitle: Text(
          invoice.customerName,
          style: theme.textTheme.bodyMedium,
        ),
        trailing: Text(
          '${invoice.totalAmount} ريال',
          style: theme.textTheme.titleSmall?.copyWith(
            color: theme.colorScheme.primary,
            fontWeight: FontWeight.bold,
          ),
        ),
        onTap: onTap,
      ),
    );
  }
}
```

**إرشادات Flutter:**
- استخدم `dart format` لتنسيق الكود
- اتبع [Dart Style Guide](https://dart.dev/guides/language/effective-dart/style)
- استخدم `const` constructors عند الإمكان
- اكتب widget tests للمكونات
- استخدم Riverpod لإدارة الحالة

### رسائل Commit

استخدم [Conventional Commits](https://www.conventionalcommits.org/):

```bash
# للميزات الجديدة
git commit -m "feat: add ZATCA invoice validation"

# للإصلاحات
git commit -m "fix: resolve authentication token expiry issue"

# للوثائق
git commit -m "docs: update API documentation for invoices"

# للتحسينات
git commit -m "perf: optimize database queries for invoice listing"

# للتغييرات في الأسلوب
git commit -m "style: format code according to style guide"

# للاختبارات
git commit -m "test: add unit tests for invoice service"
```

## 🔍 عملية المراجعة

### مراجعة الكود

1. **المراجعة الآلية**: تشغيل CI/CD checks
2. **مراجعة الفريق**: مراجعة من عضوين على الأقل
3. **اختبار الوظائف**: التأكد من عمل الميزة كما هو متوقع
4. **مراجعة الوثائق**: التأكد من تحديث الوثائق

### معايير القبول

- ✅ جميع الاختبارات تنجح
- ✅ تغطية الكود > 80%
- ✅ لا توجد مشاكل أمنية
- ✅ الكود يتبع معايير المشروع
- ✅ الوثائق محدثة
- ✅ لا توجد تعارضات في Git

## 🐛 الإبلاغ عن المشاكل

### قبل الإبلاغ

1. تأكد من أن المشكلة لم يتم الإبلاغ عنها مسبقاً
2. جرب إعادة إنتاج المشكلة
3. اجمع معلومات النظام والبيئة

### تقرير المشكلة

استخدم [قالب Issue](https://github.com/baseer-team/baseer/issues/new/choose):

```markdown
## وصف المشكلة
وصف واضح ومختصر للمشكلة.

## خطوات إعادة الإنتاج
1. اذهب إلى '...'
2. اضغط على '...'
3. انتقل إلى '...'
4. شاهد الخطأ

## السلوك المتوقع
وصف واضح لما كنت تتوقع حدوثه.

## لقطات الشاشة
إذا كان ذلك مناسباً، أضف لقطات شاشة لتوضيح المشكلة.

## معلومات البيئة
- نظام التشغيل: [مثل iOS, Android, Windows, macOS]
- المتصفح: [مثل Chrome, Safari]
- الإصدار: [مثل 22]

## معلومات إضافية
أي معلومات أخرى حول المشكلة.
```

## 💡 طلب ميزات جديدة

### قبل الطلب

1. تأكد من أن الميزة لم يتم طلبها مسبقاً
2. فكر في كيفية تناسب الميزة مع رؤية المشروع
3. اعتبر التأثير على المستخدمين الحاليين

### قالب طلب الميزة

```markdown
## ملخص الميزة
وصف مختصر للميزة المطلوبة.

## المشكلة التي تحلها
وصف المشكلة أو الحاجة التي تحلها هذه الميزة.

## الحل المقترح
وصف واضح لما تريد حدوثه.

## البدائل المدروسة
وصف أي حلول أو ميزات بديلة فكرت فيها.

## معلومات إضافية
أي معلومات أخرى أو لقطات شاشة حول طلب الميزة.
```

## 🛠️ إرشادات التطوير

### هيكل المشروع

```
baseer/
├── backend/                 # Go Backend
│   ├── internal/
│   │   ├── api/            # HTTP handlers
│   │   ├── models/         # Data models
│   │   ├── services/       # Business logic
│   │   └── config/         # Configuration
│   └── tests/              # Tests
├── frontend/               # Flutter Frontend
│   ├── lib/
│   │   ├── core/          # Core functionality
│   │   ├── features/      # App features
│   │   └── shared/        # Shared components
│   └── test/              # Tests
└── docs/                  # Documentation
```

### تشغيل الاختبارات

```bash
# Backend tests
cd backend
make test
make test-coverage

# Frontend tests
cd frontend
flutter test
flutter test --coverage

# Integration tests
flutter test integration_test/
```

### إرشادات الأمان

- لا تضع أسرار أو مفاتيح API في الكود
- استخدم متغيرات البيئة للإعدادات الحساسة
- اتبع مبادئ الأمان في التطوير
- أبلغ عن الثغرات الأمنية بشكل خاص

### الأداء

- اختبر الأداء للميزات الجديدة
- استخدم أدوات profiling عند الحاجة
- تأكد من أن التغييرات لا تؤثر سلباً على الأداء
- اتبع best practices للأداء

## 📞 الحصول على المساعدة

### قنوات التواصل

- **GitHub Discussions**: للأسئلة العامة والمناقشات
- **GitHub Issues**: للمشاكل والطلبات المحددة
- **Discord**: للدردشة المباشرة مع الفريق
- **Email**: dev@baseer.sa للاستفسارات الخاصة

### الموارد المفيدة

- [وثائق المشروع](docs/)
- [دليل API](docs/api/)
- [أمثلة الكود](examples/)
- [الأسئلة الشائعة](docs/FAQ.md)

## 🎉 شكر المساهمين

نشكر جميع المساهمين الذين يساعدون في تطوير Baseer:

- المساهمون في الكود
- مُختبري الميزات
- كاتبو الوثائق
- مُبلغو المشاكل
- مقترحو الميزات

## 📄 الترخيص

بمساهمتك في هذا المشروع، فإنك توافق على أن مساهماتك ستصبح ملكية خاصة لشركة Baseer Technologies وتخضع لحقوق الطبع والنشر المحفوظة. راجع [COPYRIGHT.md](COPYRIGHT.md) للتفاصيل.

---

**شكراً لك على اهتمامك بالمساهمة في Baseer! 🚀**

نتطلع إلى رؤية مساهماتك وجعل Baseer أفضل نظام تشغيل مالي للمنطقة العربية.