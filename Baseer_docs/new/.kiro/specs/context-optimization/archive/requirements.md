# Requirements Document: تحسين إدارة السياق

**المشروع:** بصير MVP  
**التاريخ:** 7 ديسمبر 2025  
**المؤلف:** فريق وكلاء تطوير مشروع بصير  
**الحالة:** 🔄 قيد المراجعة

---

## Introduction

تواجه بيئة التطوير مشكلة متكررة في تجاوز حد السياق (Context limit exceeded) بسبب تحميل كميات كبيرة من المعلومات في كل طلب. هذا يؤثر على كفاءة العمل ويتطلب إعادة بدء الجلسات بشكل متكرر.

## Glossary

- **Context**: السياق - المعلومات المحملة في ذاكرة الوكيل
- **Steering Files**: ملفات التوجيه - ملفات المعايير والتوجيهات في `.kiro/steering/`
- **Token Budget**: ميزانية الرموز - الحد الأقصى للمعلومات المسموح بها
- **System Prompt**: التعليمات النظامية - التعليمات الأساسية للوكيل

---

## Requirements

### Requirement 1: تقليل حجم ملفات التوجيه

**User Story:** كمطور، أريد أن تكون ملفات التوجيه مختصرة وفعالة، حتى لا تستهلك معظم السياق المتاح.

#### Acceptance Criteria

1. WHEN يتم تحميل ملفات التوجيه THEN يجب ألا يتجاوز حجمها الإجمالي 30% من ميزانية السياق
2. WHEN يوجد محتوى مكرر في ملفات متعددة THEN يجب دمجه في ملف واحد
3. WHEN يوجد محتوى غير ضروري THEN يجب إزالته أو نقله لملفات مرجعية
4. WHEN تكون المعلومات طويلة جداً THEN يجب تلخيصها مع الإبقاء على النقاط الأساسية
5. WHEN يتم تحديث ملف توجيه THEN يجب مراجعة حجمه والتأكد من عدم تجاوز الحد المسموح

### Requirement 2: تحسين بنية ملفات التوجيه

**User Story:** كمطور، أريد أن تكون ملفات التوجيه منظمة بشكل هرمي، حتى يتم تحميل المعلومات الضرورية فقط.

#### Acceptance Criteria

1. WHEN يوجد ملف توجيه كبير THEN يجب تقسيمه إلى ملف رئيسي مختصر وملفات مرجعية تفصيلية
2. WHEN يحتاج الوكيل لمعلومات تفصيلية THEN يجب أن يطلبها بشكل صريح من الملفات المرجعية
3. WHEN يتم إنشاء ملف توجيه جديد THEN يجب أن يتبع البنية الهرمية المحددة
4. WHEN توجد معلومات نادرة الاستخدام THEN يجب نقلها لملفات مرجعية منفصلة
5. WHEN يتم تحميل ملف توجيه THEN يجب تحميل النسخة المختصرة فقط بشكل افتراضي

### Requirement 3: إدارة ذكية للسياق

**User Story:** كمطور، أريد أن يدير النظام السياق بذكاء، حتى يتم استخدام المساحة المتاحة بكفاءة.

#### Acceptance Criteria

1. WHEN يقترب السياق من الحد الأقصى THEN يجب تحذير الوكيل قبل الوصول للحد
2. WHEN يتم فتح ملفات كثيرة في المحرر THEN يجب تحميل الملفات الأكثر صلة فقط
3. WHEN يتم طلب معلومات من ملف كبير THEN يجب تحميل الأجزاء ذات الصلة فقط
4. WHEN يتم إنشاء تقرير أو وثيقة THEN يجب أن تكون مختصرة وتتجنب التكرار
5. WHEN يتم تحميل معلومات تاريخية THEN يجب تحميل الملخصات فقط وليس التفاصيل الكاملة

### Requirement 4: تحسين التعليمات النظامية

**User Story:** كمطور، أريد أن تكون التعليمات النظامية مركزة وفعالة، حتى لا تستهلك مساحة كبيرة من السياق.

#### Acceptance Criteria

1. WHEN يتم كتابة تعليمات نظامية THEN يجب أن تكون مختصرة ومباشرة
2. WHEN توجد أمثلة في التعليمات THEN يجب أن تكون قصيرة وتوضيحية فقط
3. WHEN توجد قواعد متعددة THEN يجب تجميعها في نقاط واضحة
4. WHEN توجد معلومات مكررة THEN يجب الإشارة للمصدر بدلاً من التكرار
5. WHEN يتم تحديث التعليمات THEN يجب مراجعة الطول والتأكد من الاختصار

### Requirement 5: مراقبة استخدام السياق

**User Story:** كمطور، أريد أن أعرف كيف يتم استخدام السياق، حتى أتمكن من تحسينه.

#### Acceptance Criteria

1. WHEN يبدأ الوكيل العمل THEN يجب عرض نسبة استخدام السياق الحالية
2. WHEN يتم تحميل ملف كبير THEN يجب عرض حجمه وتأثيره على السياق
3. WHEN يقترب السياق من الحد THEN يجب عرض تحذير واضح
4. WHEN يتم تجاوز الحد THEN يجب عرض تقرير بالعناصر الأكثر استهلاكاً
5. WHEN يتم طلب تحسين THEN يجب تقديم اقتراحات محددة لتقليل الاستخدام

### Requirement 6: إنشاء ملفات مرجعية

**User Story:** كمطور، أريد أن تكون المعلومات التفصيلية في ملفات مرجعية منفصلة، حتى يتم تحميلها عند الحاجة فقط.

#### Acceptance Criteria

1. WHEN توجد معلومات تفصيلية في ملف توجيه THEN يجب نقلها لملف مرجعي
2. WHEN يحتاج الوكيل لمعلومات تفصيلية THEN يجب أن يطلب الملف المرجعي بشكل صريح
3. WHEN يتم إنشاء ملف مرجعي THEN يجب أن يكون منظماً وسهل البحث
4. WHEN يتم تحديث معلومات THEN يجب تحديث الملف المرجعي المناسب
5. WHEN يتم البحث عن معلومة THEN يجب أن يكون الملف المرجعي قابلاً للبحث بسهولة

---

## Priority

**عالية جداً** - هذه المشكلة تؤثر على كفاءة العمل بشكل مباشر وتتطلب حلاً عاجلاً.

---

## Success Metrics

- تقليل حجم ملفات التوجيه بنسبة 60%+
- تقليل حالات تجاوز السياق بنسبة 90%+
- زيادة كفاءة استخدام السياق إلى 80%+
- تقليل الحاجة لإعادة بدء الجلسات بنسبة 80%+

---

**تم إعداده بواسطة:** فريق وكلاء تطوير مشروع بصير  
**التاريخ:** 7 ديسمبر 2025  
**الإصدار:** 1.0  
**الحالة:** 🔄 قيد المراجعة
